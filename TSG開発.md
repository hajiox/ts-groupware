# TSG開発メモ（現行版）

## 最終更新
2026-08-29

## 2026-08-29 石井瑞季のフロア勤務時間訂正
- 石井瑞季（社員NO 92）は `5日正社員 / フロア` だが、個人別基本勤務が未登録で、8月の「基本勤務」23件へフロア共通候補 `09:00-18:00` が流用されていた。実打刻は8:20台の出勤・17:30前後の退勤が中心で、本人の基本勤務 `08:30-17:30` と一致することを確認した。
- 個人別基本勤務を `08:30-17:30 / 休憩60分 / 実勤務480分` として2026年8月1日から登録し、同日以降の基本勤務23件を同じ時刻へ補正した。会社休・希望休・忌引き休・手動の別時刻は変更していない。
- 本番マイグレーション `202608290001_ishii_floor_work_time.sql` を適用し、人事設定、確定シフト、連動する有給・勤怠確認スナップショットを同時に整合させた。本番DBと管理画面で23件一致、旧 `09:00-18:00` とその他不一致はいずれも0件を確認した。

## 2026-08-27 藤田香織のフロア勤務時間訂正
- 藤田香織（社員NO 9）の個人別基本勤務を `11:00-18:30 / 休憩60分 / 実勤務390分` として登録した。6日正社員の所定6.5時間と一致する。
- フロアの共通勤務候補 `09:00-18:00` が個人別設定未登録の藤田さんへ流用され、8月25日などを遅刻と誤判定していた。2026年8月1日以降の「基本勤務」系・「フロア勤務」25件を訂正し、連動する有給・勤怠確認の所定時間スナップショットも更新した。
- AI下書き、手動の勤務候補選択、API保存の全経路で、個人別基本勤務が設定された正社員の基本勤務系・フロア勤務系に本人時刻を優先するよう統一した。共通候補そのものは他スタッフ向けに変更していない。
- 本番で人事設定 `11:00 / 18:30 / 60分`、藤田さんの本人回答0件・遅刻早退0回を確認。対象ESLintはエラー0、`tsc --noEmit`、全体Lint（既存警告のみ）、ローカルビルドに成功した。

## 2026-08-27 旧有給管理掲示板の閲覧専用化
- 2026年8月1日の専用有給システム開始後も「TS（有給管理）」掲示板へ有給申請が3件投稿されていたため、同掲示板を履歴閲覧専用へ変更した。DBトリガーで新規投稿・コメント・本文/添付編集を拒否し、APIでも403を返す。画面から投稿・コメント欄を外し、「管理」→「有給申請」への導線を表示する。
- 投稿者は佐藤葵（フロア）と武藤志保の2名。佐藤葵の8月13日全休・14日半休は専用システムで承認済みのため再申請不要と案内した。武藤の8月28日は確定シフト上で休み希望・勤務枠なしのため、所属管理者へ勤務日変更を連絡後に専用申請する手順を案内した。
- TSG君から両名へ個別DMを各1件送信し、同一本文の重複防止、DMメンバー、送信者、対象掲示板の制度開始後投稿をマイグレーション内で検証した。有給取得マニュアルにも旧掲示板では申請にならない旨を追記した。
- マイグレーション `202608270001_group_posting_policy.sql` と `202608270002_notify_paid_leave_board_applicants.sql` を本番適用。DB上の投稿拒否、対象掲示板のロック、個別DM各1件を確認した。対象ESLint、`tsc --noEmit`、ローカルビルドに成功。

## 2026-08-25 フルタイムパート遅刻時の現行挙動確認
- 実打刻と確定シフトの開始時刻を比較する遅刻判定は未実装で、1時間遅れて出勤しても遅刻回数・遅刻時間・勤怠備考は自動登録されず、実際の出勤時刻が保存される。
- 時給計算プロファイルでは、実打刻を15分単位で丸め、休憩控除後の勤務分だけ給与計算する。休憩区分の境界をまたぐ場合は休憩控除も変わるため、1時間遅刻しても正味の減少時間が必ず1時間になるとは限らない。
- `遅刻早退回数`・`遅刻早退時間`・`遅早控除`は労務士データの取込項目としては存在するが、現行の社内打刻からは自動算出していない。今回は調査記録のみで、計算仕様は変更していない。

## 2026-08-24 本番機能欠落の緊急復旧とGitHub正本化
- 2026-08-24 18:59に、勤怠・管理・シフト等の現行ソースを含まないGitHub版が本番へ自動デプロイされ、現行機能を上書きした。データ削除ではなく、欠けたソースによる本番置換が直接原因。
- 本番を直前の正常deployment `dpl_8jfmZqvKeiwFGzugdWmDquTohNPN` へ即時ロールバックし、管理9機能、勤怠管理、本社・道の駅タイムレコーダー、シフト作成を実画面とAPIで復旧確認した。
- GitHub最新`main`の新規cloneへ正常稼働版の未収録機能を統合し、最新のアカウント単位既読、TSA販売価格・EC商品名報告も保持した。既読はグループ参加日時を下限にし、参加前の全投稿を未読に数える異常値を防ぐ。
- 菅山紗来さんは仮入社社員NO149とLINE承認時の自動生成NO150に二重化していた。NO149へ本LINE、今日の打刻、シフト、希望、履歴書を統合し、NO150と仮アカウントを削除した。今後は承認前に本名・所属を必須とし、承認後に本名を設定した場合も仮入社行へ統合する。
- Supabase整合性監査では、重複社員、孤立打刻、有給割当不整合、確定シフト重複はいずれも0件。既存の7月打刻には出退勤数の要確認日が2日残るため、今回の復旧とは分けて勤怠管理で確認する。
- `pnpm run test:account-read-status`、TSA価格・商品名通知テスト、ESLint（エラー0）、`tsc --noEmit`、`next build`（79ルート）に成功した。
- 復旧後、藤田香織さんと渡部瞳さんへ、システム更新時の不備と勤怠・管理・シフトの復旧完了をTSG君から個別DMで簡潔に報告した。

## 2026-08-18 道の駅シフト人数の勤務先・Timee集計
- 道の駅人数は、勤務ラベルが「本社勤務」のスタッフを除外し、Timeeの予定人数を加算するよう変更した。Timee人数は保存済み人数を優先し、自由欄の「1名」「2人」等も解釈し、内容または時刻だけが入っている場合は1名として扱う。
- 確定シフト画面、印刷プレビュー、Excel出力、不足警告、AI下書きの必要人数判定を同じ集計ルールへ統一した。全休の有給は従来どおり人数に含めない。
- 本番の道の駅8月シフトで、8月1日は本社勤務1名を除外して4名、8月2日はスタッフ3名とTimee1名で4名となることを確認。対象ESLint、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功し、ブラウザエラー0件を確認した。
- 本番デプロイ `CS4fW15YET4tKFsMSufmqS1gDsvq` を `https://v0-line-blush.vercel.app` に反映した。
- 修正ルールと本番確認完了を佐藤ちさとさんの個別DMへ送信し、送信本文の表示とブラウザエラー0件を確認した。

## 2026-08-18 スマホ確定シフトの最終行表示
- iPhoneのホーム画面表示では下部セーフエリア分だけ固定メニューが高くなる一方、確定シフト表の高さ計算がその分を差し引いておらず、最終行の下端がメニュー下へ入る問題を修正した。
- スマホの表高さを `safe-area-inset-bottom` 対応にし、小さい画面で高さ260pxを強制していた式も `clamp` に変更。PC表示と横向き全画面表示には変更を加えていない。
- 対象ESLint、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功。本番の幅390px通常端末・小型端末相当で最終行が固定メニューより上に表示され、ブラウザエラー0件を確認した。
- 本番デプロイ `EPU7W3crp6Wb8fvrEuvGhtarcVp5` を `https://v0-line-blush.vercel.app` に反映した。

## 2026-08-16 シフト印刷プレビューのフロア人数セル色
- 人数不足時に印刷側だけで自動付与していた薄橙色を廃止した。人数セルは作成画面で明示指定した色だけを反映し、未指定は白で表示する。
- ESLint、`tsc --noEmit`、ローカルビルド、本番Vercelビルドに成功。本番のフロア印刷プレビューで全11日分の未指定人数セルが白であることを確認した。
- 本番デプロイ `B7kGTyLuVtSKSPieJYMuRhF5MyaM` を `https://v0-line-blush.vercel.app` に反映した。

## 2026-08-16 藤田香織さんへの修正完了DM
- TSG君から藤田香織さんの個別DMへ、人数不足時の自動着色廃止、未指定時の白表示、指定色のみ反映、本番確認完了を報告した。
- 本番マイグレーション `202608160102_notify_fujita_shift_print_count_color.sql` を適用し、送信者・宛先・DMメンバー・本文をトランザクション内で検証した。本文一致による重複送信防止付き。

## 2026-08-10 小島遼 基本勤務時間変更
- 製造・社員NO142 小島遼の基本勤務を、2026-08-11以降 `12:30-17:30 / 休憩0分` から `11:30-16:30 / 休憩0分` へ変更した。
- 確定済み8月前半シフトは旧基本勤務と完全一致した8日（8/11〜14、8/17〜20）のみ更新。8/15希望休、8/16会社休、8/10以前、別時刻の手動割当は変更していない。
- 人事基本勤務を将来の製造AI下書き参照元として更新。勤務時間は変更前後とも300分のため、勤務日数・有給参考時間・給与時間に差は生じない。
- `pnpm lint` と `pnpm build` に成功。本番画面で変更前10日間の維持と変更後8日間、休憩0分を照合した。既存の型チェックエラー3件は今回変更外。

## 2026-08-07 労務士給与との比較画面整理
- 労務ZIP取込後に全ファイルを縦へ並べる表示を廃止。取込履歴・原文ファイルは監査用の折りたたみに移し、通常は最新8回・原文24件だけを表示する。
- 労務士確定額と社内計算を社員別・項目別に比較し、比較不能要因、基本給、残業、課税手当、非課税支給、控除、端数処理の変更ポイントを優先度・対象社員・差額規模・変更先付きで表示する。
- 社員別根拠は未解決者を12人ずつ折りたたみ表示し、一致者は初期非表示とした。2026年6月の本番データでは比較可能23/41人、主要項目一致0人、支給差合計39,505円を確認した。
- ZIP登録時に対象の給与期間を作成し、新規月が過去の最新比較月へ誤って戻らないよう修正。受付直後は「原文受付・解析待ち」と明示する。
- 新規ZIP内のPDF・Excel・CSVを自動で給与結果へ変換するサーバー解析処理は未実装。現状は原文とファイル索引の登録までで、解析済みデータが入った段階から比較へ反映される。
- 対象ESLint、`pnpm build`、Vercel本番ビルドに成功。本番 `https://v0-line-blush.vercel.app` で横はみ出しなし、取込履歴の初期折りたたみ、比較カード、ブラウザエラー0件を確認した。デプロイ: `2ZAae4xZnG2C83ceSFfGV4ougD2Q`。
- 旧取込経路で給与期間が作成されなかったZIPも、労務バッチの勤務対象月・支給月・支給日から自動補完するよう修正。画面表記を「2026年7月勤務分 / 8月10日支給」に統一し、支給月と勤務対象月の入力例を追加した。本番の最新ZIPは同表記で先頭表示され、原文18件が解析待ちであることを確認した。
- 労務士名・旧姓・社内通名を社員マスタ別名として登録する突合機能を追加。森 結芽香の労務士名「鈴木 結芽香」を正式に紐付け、元の取込専用社員行は履歴を残したまま無効化した。2026年6月支給分で候補表示と重複行が消え、森本人へ労務士結果が付くことを確認。対象ESLint・`pnpm build`・Vercel本番ビルド成功、ブラウザエラー0件。デプロイ: `HvFdi8JsUTu1wTgNL1jiCmdg5Nci`。

## 2026-07-27 所属別の勤務候補マスタ
- シフト作成画面の所属タブ直下に、フロア・製造・道の駅それぞれ独立した勤務候補の編集欄を追加。
- 表示名、用途、出勤・退勤時刻、休憩分、表示順を編集でき、新規追加と候補削除にも対応。候補削除後も保存済みシフトの表示は保持する。
- 表示名とは別に `pattern_role` を保持し、「基本勤務」などの名称を変更しても正社員向け・フロア不足時勤務・AI下書きの判定が壊れないようにした。

## 2026-07-27 有給管理の直近平均勤務時間
- 正社員の直近平均から、タイムレコーダー本運用前（2026-06-16より前）に取り込まれた残業部分だけの打刻を除外。
- 日別集計と実打刻が同日にある場合は、管理修正が反映される実打刻を優先。
- 休憩控除の表示を「5時間超30分・6時間超45分・8時間超60分」と明記。

## 2026-07-18 ECセール開始・終了時刻
- ECセールの開始・終了時刻は名称マスタではなく、各日付の「ECセール」選択画面で入力する日別情報へ変更。同じセールでも日付ごとに異なる時刻を保持する。
- 日別シフトの選択済みチップ、印刷プレビュー、Excel出力では「セール名 20:00〜01:59」の形式で表示する。時刻未設定の既存項目は従来どおり名称だけ表示する。
- 日付セルの色は勤務内容の意味を持つセル色パレットから分離。各日付の専用8色パレットで設定・解除し、印刷にも同じ色を反映する。
- 日付以外の通常セルも上部で色モードを選ぶ方式を廃止。各セルのペイントボタンからプリセット色・任意色・色なしを直接選ぶカラーパレットへ変更した。
- 日付セルは任意色調合を廃止し、赤・オレンジ・黄・緑・水色・青・紫・グレーの明確な代表8色と「色なし」から選ぶ専用パレットに変更した。赤などが淡く見えない標準色を使う。
- 日付パレットを開いた行の固定セルを最前面へ上げ、次の日付行のボタンや罫線がパレット上へ重なる問題を修正。日付パレットは同時に1つだけ開く。

## 2026-07-18 シフト変更の一時保存・確定保存
- シフト入力中の勤務割当、管理者による休み変更、必要人数、場所、Timee、備考、ECセール、作業予定、セル色を画面内に一時保持し、「変更をキャンセル」「一時保存」「確定保存」でまとめて処理する方式へ変更。
- 一時保存は状態を「調整中」として再編集可能。確定保存は確認ダイアログ後に状態を「確定」とし、管理画面とAPIの両方で変更・削除・AI再生成・希望修正を拒否する。
- 未保存状態で所属や別シフトへ移動する場合は確認を表示し、誤って編集内容を失わないようにした。

## 2026-07-18 シフト対象除外・ナビ・Chat作成UI修正
- シフト期間ごとの対象外スタッフを保持する `gw_shift_period_exclusions` を追加。管理画面の「シフト対象スタッフ」から除外でき、当該期間の希望・割当・希望回収対象を削除する。人事情報と過去シフトは保持する。
- 除外したスタッフはAI下書き、シフト表、本人の希望画面、Excel、印刷から外れる。誤操作時は「除外済みスタッフ」から対象へ戻せる。
- 管理画面のChat作成ではアイコン選択を表示せず、吹き出しアイコンへ固定した。掲示板作成時のみアイコンを選択できる。
- 通常画面の下部ナビから勤怠ボタンを削除し、勤怠管理は管理画面内へ集約した。

## 2026-07-18 希望回収テストのランダム生成
- `希望回収テスト` は管理者やスタッフへ入力画面を配信せず、選択中の所属スタッフ全員分の休み希望、最大出勤日数、希望出勤日数、最低休日数、最大連勤、自由備考を整合する範囲でランダム生成する検証専用機能とした。
- 生成したテスト希望はAI下書きへそのまま反映する。再実行時は前回のテスト希望とAI割当だけを入れ替え、通常希望・手動割当は保護する。実希望または通常回収対象がある期間では実行を拒否する。
- テスト期間はスタッフのホームと希望回収画面へ一切表示しない。管理者選択チェックUIを削除し、ボタン1回で生成できる元の操作へ戻した。

## 2026-07-17 グループ所属管理修正
- TSG管理者権限と掲示板・グループChatの所属を分離し、管理者も `gw_group_members` に明示登録されているグループだけ通常画面へ表示する仕様に変更した。
- 管理画面では所属外を含む全グループを管理できる専用APIを使用し、管理者を含む任意メンバーの追加・除外を可能にした。除外後は通常一覧、メンバー一覧、投稿・Chat APIからもアクセス不可となる。
- 全員追加でグループを作成した場合も、グループ管理者は作成者だけとし、他のTSG管理者は通常メンバーとして追加する。
- グループ削除は関連データの外部キーCASCADEを利用する単一削除に整理し、失敗時は管理画面へ実エラーを表示する。

## 2026-07-15 Google Drive添付アップロード認証復旧
- 履歴書OCR対応時に本番へ反映したDrive OAuth更新トークンが `invalid_grant` となり、掲示板・チャット等の添付アップロードが失敗していた。
- 保存先を所有する `aizubrandhall@gmail.com` でDrive権限を再認証し、本番Vercelの `GOOGLE_DRIVE_REFRESH_TOKEN` を更新。直前の本番ソースを再デプロイしてエイリアスへ反映した。
- 新認証で保存先フォルダへのテストファイル作成・削除に成功。ローカルの失効トークンも更新し、トークンを含む一時ログは削除した。

## 2026-06-18 セッション更新
- TSGログインセッションを30日スライド延長から7日固定期限へ変更。ログイン時刻をCookieに含め、7日経過後は必ずLINE再ログインが必要。
- 旧形式の `gw_user_session` Cookie は発行時刻がないため無効扱いにし、再ログインで新形式へ更新する。Proxy、トップページ、APIのセッション取得で同じ期限判定を使う。

## 2026-06-18 更新
- LINEアイコンURLから `tsg_line_sync` を除去し、主要画面のアバターを画像404時に文字アイコンへ退避する共通表示へ変更。再ログイン時にLINEプロフィール画像URLを更新する。
- DocScanner eFAX受信APIでTSG側でもFAX画像をJPEG正規化し、余白/インク位置から上下逆を検知した場合は180度回転してDriveへ保存する。既存の逆向きFAX投稿は同じ受信IDで再処理して添付を差し替え済み。

## 役割
TSGは、社内掲示板、グループChat、DM、PWA通知を担当する社内グループウェア。

## 場所とURL
- リポジトリ: `C:\作業用\ts-groupware`
- 本番: `https://v0-line-blush.vercel.app`
- Google Drive同期MD: `G:\マイドライブ\md\TSG開発.md`

## ログイン
- LINEログインを使用する。
- iPhoneで通知を使う場合は、Safariで開いてホーム画面に追加したPWAからログインする。
- ChromeなどiOSの他ブラウザではWeb Push通知が実用対象外。

## 通知
- 投稿者本人には本人投稿の通知を送らない。通知されない場合でも、本人投稿なら正常。
- 掲示板、グループChat、DMは通知生成対象。
- 実際に届くかは、端末ごとのWeb Push購読、PWA化、通知ミュート設定に依存する。
- DMは相手にだけ通知する。TSG君（AI）がDMで返答した場合も質問者へ通知する。
- 未読数は下部ナビ、DM一覧、PWA App Badgeへ反映する。
- 2026-06-13 未読数の表示基準を端末単位からアカウント単位へ戻した。端末別購読情報はPush送信用に残すが、未読集計と既読更新は `gw_read_status` を使う。
- 2026-06-13 勤怠打刻は業務場所に設置した専用タイムレコーダー端末のみで行う運用に統一。通常スマホの下部ナビから「勤怠」を削除し、`/attendance` はホームへリダイレクト、`/api/attendance/time-clock` は 410 で停止。専用端末 `/time-clock/[deviceKey]` と管理画面の勤怠管理は継続。
- 2026-06-14 専用タイムレコーダーの退勤確認画面で、青い退勤ボタンを上、場所・距離・備考・30分休憩ボタンを下に並ぶ順へ変更。

## PWA / iPhone
- `public/manifest.json`、apple touch icon、192/512アイコンを設定済み。
- iOSではSafari PWAのみWeb Pushが有効。
- 設定画面では、PWA外の場合にホーム画面追加手順を案内する。
- PWA App Badgeは全体未読APIをポーリングし、DMとグループの合計未読数を反映する。

## favicon / OGP
- 会津ブランド館系の共通ファビコン画像を設定。
- OGP画像も設定し、共有時・ブラウザタブ・PWAで表示が崩れないよう整理。
- 関連ファイル:
  - `app/layout.tsx`
  - `public/manifest.json`
  - `public/favicon.png`
  - `public/og-image.png`
  - `public/apple-touch-icon.png`
  - `public/icon-192.png`
  - `public/icon-512.png`

## 管理画面
- 管理画面ではユーザー管理とグループ管理を行う。
- ユーザー管理ではLINE表示名とは別に本名 `real_name` を設定でき、本名がある場合はアプリ全体で本名を優先表示する。
- グループ一覧はホームのグループカードと同様に色分けする。
  - 掲示板: 青系
  - グループChat: 緑系
- 2026-05-28時点の管理画面色分け変更:
  - `app/admin/page.tsx`
  - `app/globals.css`
- `npm run build` 成功。`npm run lint` はローカルで `eslint` コマンド未解決。

## データ/API
- Supabase DBは内職管理システム系と共用している箇所があるため、DB変更は影響範囲を確認してから行う。
- `gw_users.real_name` は本名表示用。
- `gw_read_status` は既読・未読判定用。
- `/api/unread` はDMとグループの合計未読数を返す。
- `/api/groups` はN+1クエリを避けるため一括取得方式。

## 運用メモ
- 端末別の通知挙動を確認する時は、PCで開いている状態とPWA端末の既読状態を分けて見る。
- 投稿編集・削除では通知を送らない。
- グループ作成時、掲示板とChatの種別を明確にし、管理画面でも見分けられるようにする。

## 削ったもの
- 古いコメントフォーム修正、iPhone通知検証の試行錯誤、細かいUI履歴は削除。
- 必要な詳細はGit履歴またはGoogle Drive版履歴を参照する。

## 2026-06-05 更新
- タスク依頼機能を追加。投稿時にタスク依頼をONにし、担当者を複数選択、期限を設定できる。自分を自動選択しない。自分宛タスクは自分で選んだ時だけ忘備録として使う。
- タスク依頼投稿は通常投稿と同じくグループメンバーに表示し、タスク依頼バッジを付ける。未完了タスクがあるユーザーにはタイムライン上部に案内を出し、タスク一覧で完了できる。
- 2026-06-13 タスク依頼投稿の編集で、本文・期限・担当者を変更可能にした。担当者から外した行は削除せず `gw_tasks.canceled_at` でキャンセル履歴として保持し、未完了一覧と投稿表示からは除外する。
- 掲示板、掲示板コメント、グループChat、DMのメンションを整備。メンション名は「さん」付きで挿入し、メンションされた投稿がある通知を出す。
- 掲示板/Chatグループ/DMで画像添付のコピー&ペーストに対応。グループChatには掲示板同様のリアクションを追加。
- DM個人Chatは自分の投稿を編集可能。グループChatは自分の投稿を編集/削除可能、管理職は全投稿を編集/削除可能。
- 検索機能を掲示板とChatグループに追加。
- スマホUIを継続調整。下部メニューはLINEライクに固定し、Chat入力欄/投稿欄と重ならないようにする。Androidスクロール不能報告に対して、本文を通常スクロールへ戻し、Pull-to-refreshの横取りと固定バーのtransformを調整済み。
- 横流れ防止を全体へ適用。PC版は幅600px程度で中央寄せ、スマホ版は横スクロールを出さない。
- フォントサイズ設定は2段階のみ。現在の「大きめ」を標準サイズ相当にし、もう一段大きい設定を用意。
- DocScanner/TSA連携APIを整備。DocScanner EC速報、DocScanner eFAX受信、TSA売上報告をTSG掲示板へ外部連携投稿できる。投稿者は原則TSG君または選択したユーザー。
- 2026-06-05時点で本番 `https://v0-line-blush.vercel.app` へデプロイ済み。

## 2026-06-06 更新
- 掲示板投稿へのコメント作成時も、通常投稿と同じくグループ所属者へ通知するように変更。本人、グループ通知OFF、投稿単位通知OFF、メンション重複除外は維持する。
- コメント先の親投稿が同一グループ内の通常投稿であることをAPI側で確認し、不正な親投稿IDやコメントへの返信は拒否する。
- カレンダーにGoogle Calendar取込を追加。`aizubrandhall@gmail.com` の予定を表示中の月範囲で読み込み、Google予定IDで重複を防いでTSG予定へ反映する。
- Google取込には `gw_calendar_events.external_id` / `source_updated_at` 追加マイグレーションと、`GOOGLE_CALENDAR_ID` / `GOOGLE_CALENDAR_REFRESH_TOKEN` または共有済みサービスアカウントが必要。
- グループチャットのAndroidスクロール対策として、入力欄を固定バーからチャット画面内の `chat-footer` に移動。チャット本文はページ本体の自然スクロールへ戻し、`/chat` ではPullToRefreshを無効化。`npm run lint` / `npm run build` は成功、`npx tsc --noEmit` は既存のadmin/board/link-preview/supabase型エラーで失敗。
- グループチャットを開いた瞬間に最下部へ高速スクロールする原因だった無条件 `scrollIntoView` を撤去。初回表示とポーリング重複ではスクロールせず、自分の送信時または最下部付近にいる時の新着のみ下へ追従する。`chat-footer` は sticky も外して通常フローのフッターに変更。
- 上記チャットスクロール修正を `vercel --prod` で本番デプロイ。Production URL は `https://ts-groupware-qvet85h7r-hajioxs-projects.vercel.app`、本番エイリアス `https://v0-line-blush.vercel.app` へ反映済み。
- チャット入力欄が通常フローで流れてしまう問題を再修正。`chat-page` を下ナビ分を除いた固定高さflexにし、ヘッダー/検索/フッターは固定段、本文 `.chat-messages` だけを内部スクロールに変更。初期表示は本文スクロールを同期的に最下部へ配置し、高速アニメーションは使わない。`vercel --prod` で `https://v0-line-blush.vercel.app` へ再デプロイ済み。
- メッセージ入力フォーカス時の微妙な左右ズレ/拡大対策として、チャットtextareaのフォントサイズを `max(16px, calc(13px * var(--font-scale)))` に上書き。モバイルブラウザの16px未満入力ズームを避ける。`npm run lint` / `npm run build` / `vercel --prod` 成功、本番URL 200確認済み。
- 掲示板もグループチャットと同じ方針でフッター化。`board-page` を下ナビ分を除いた固定高さflexにし、ヘッダー/検索/`board-footer` は固定段、`.post-list` だけ内部スクロールへ変更。`post-input-bar` の fixed 配置を撤去し、投稿textareaも16px未満入力ズーム回避対象に追加。`npm run lint` / `npm run build` / `vercel --prod` 成功、本番URL 200確認済み。
- 掲示板スマホ表示が崩れたため、掲示板の `board-page` / `board-footer` 化は撤回。掲示板は元の `post-input-bar` fixed 配置と通常 `post-list` に復旧し、投稿textareaの16px未満入力ズーム回避だけ維持。`npm run lint` / `npm run build` / `vercel --prod` 成功、本番URL 200確認済み。

## 2026-06-07 更新
- Google Calendar取込エラーは、対象カレンダー `aizubrandhall@gmail.com` ではなく認証に使うGoogle Cloud project側で Calendar API が未有効だったため発生。該当 project とAPI有効化リンクを画面に表示するように変更。
- ローカルのサービスアカウント権限では Service Usage API 経由の有効化が 403 で不可だったため、Google Cloud Consoleで対象projectの Calendar API を有効化する必要あり。`npm run lint` / `npm run build` / `vercel --prod` 成功、本番URL 200確認済み。
- Calendar API有効化後の `Not Found` は、TSGが使うサービスアカウント `billing-viewer@tsai-460605.iam.gserviceaccount.com` から `aizubrandhall@gmail.com` のカレンダーが見えていない状態。Googleカレンダー側でこのメールアドレスを「特定のユーザーとの共有」に追加する必要あり。画面にも対象カレンダー、Cloud project、TSG認証アカウントを表示するよう改善し、`vercel --prod` で本番反映済み。
- `aizubrandhall@gmail.com` のGoogleカレンダー共有に `billing-viewer@tsai-460605.iam.gserviceaccount.com` を「予定の表示（すべての予定の詳細）」で追加。サービスアカウントからCalendar API直読みOKを確認。
- Google取込APIのSupabaseエラーが汎用文言に潰れていたため、非Errorオブジェクトの `message` と非JSONレスポンスのHTTP本文を画面へ出せるよう改善。`sql/011_google_calendar_import.sql` を本番Supabaseへ適用し、`external_id` / `source_updated_at` / `source,external_id` unique index を追加。TSG本番で `Googleから60件取り込みました` を確認済み。
- 掲示板本文の自動リンク表示を短縮化。Indeed応募通知の `応募内容を確認する:` 行は画面上 `開く` と表示し、href/titleには元URLを保持する。長いURLはドメイン/先頭パスのみ表示してカード崩れを防ぐ。`npx eslint components/link-preview.tsx --quiet` / `npm run build` 成功。
- Googleカレンダー共有の `billing-viewer@tsai-460605.iam.gserviceaccount.com` を「予定の変更」へ上げ、サービスアカウント読み取りで予定色 `colorId` が返ることを確認。TSG本番の再取込で `Googleから60件取り込みました（色反映57件）` を確認済み。
- TSGカレンダーをGoogleカレンダー寄りに調整。手動予定の色はGoogle予定色パレット相当のHEXを保存可能にし、編集フォームに開始日/終了日を追加。月表示は週単位のイベントバーに変更し、複数日予定は日をまたいで横につながるバーとして表示する。`npm run lint` / `npm run build` / `vercel --prod` 成功、本番 `https://v0-line-blush.vercel.app` へ反映済み。

## 2026-06-08 更新
- ホームも掲示板/Chatと同じ固定高さflex構造へ寄せた。`groups-page` を下ナビ分を除いた高さにし、ヘッダーは固定段、`.groups-list` だけ内部スクロールに変更。bodyスクロールで下メニューが上に連れて行かれる構造を避ける。
- ホームのアカウント写真メニュー内QR表示をスマホ向けに調整。メニュー幅を画面幅基準にし、QR周辺をグリッド化、テキスト折り返しと小幅時のQR縮小で崩れにくくした。
- ホームのアカウント写真メニューに未読一括クリアを追加。2026-06-13以降は端末単位ではなくアカウント単位で `gw_read_status` を一括更新し、下ナビ未読も即時再取得する。
- 掲示板をChatと同じ安定方針へ再統一。`board-page` を下ナビ分を除いた固定高さflexにし、ヘッダー/検索/`board-footer` は固定段、`.post-list` だけ内部スクロール、投稿欄は `board-footer` 内の通常フローへ移動。`post-input-bar` はfooter内で `position: static` に上書きし、Android/iOSのキーボードと下ナビにぶつかりにくい構造にした。
- 入力欄のないホーム/タスク/DM/設定/管理などは内部スクロール化せず、下ナビ分の余白を共通化。`npm run lint` / `npm run build` / `vercel --prod` 成功。本番 `https://v0-line-blush.vercel.app` で掲示板footer、本文スクロール、その他メニュー余白を確認済み。
- 掲示板footer化後、投稿件数の多い `NEWブランド館（フロア）` で `.post-card` がflex収縮し、本文が `overflow: hidden` で消えて枠線だけ見える問題を修正。`.board-page .post-card { flex: 0 0 auto; }` を追加し、本番の該当掲示板でカード31件、先頭カード高さ907px、`.post-list` の `scrollHeight: 16750` / `scrollTop: 0 -> 650` を確認済み。
- 下メニューの管理ボタンと管理権限を再確認。管理ボタンは `/api/auth/me` の `user.role === "admin"` の場合のみ表示し、権限変更/ユーザー切替時に古い `isAdmin` が残らないよう非admin時は必ず `false` に戻す。`/admin` ページと `/api/admin/*` はadmin以外をリダイレクト/403で拒否。Googleカレンダー取込APIも管理者のみへ統一。`npm run lint` / `npm run build` / `vercel --prod` 成功、本番 `https://v0-line-blush.vercel.app` へ反映済み。
- ユーザー情報に権限とは別軸の `department`（`フロア` / `製造` / `道の駅`）を追加。`sql/012_user_departments.sql` と `supabase/migrations/202606080001_user_departments.sql` を作成し、本番DBへ適用。初期割当は `staff道の駅会津食のブランド館` 明示メンバーを `道の駅`、`TS(売上・新規・HAPPY！）` 明示メンバーを `フロア`、その他を `製造` とし、重複時は最後に道の駅が勝つ。再実行時に管理画面で手動変更済みの部署を上書きしないよう、未設定/不正値ユーザーのみseed対象にした。適用後件数は `フロア6 / 製造10 / 道の駅8`。
- 管理のユーザー一覧に部署プルダウンを追加し、`/api/admin/users` で部署取得/更新/値検証に対応。`/api/admin/members` と `/api/groups/[id]/members`、Chat APIも `department` を返すようにした。管理のグループメンバー画面では未参加者を部署単位でまとめて追加できるボタンを追加。
- 掲示板/コメント/チャットのメンションで `@フロア` / `@製造` / `@道の駅` を部署メンションとして解決し、同じ掲示板/チャット内の該当部署メンバーへ通知対象を広げる。個人メンションとの重複は1人1回にまとめる。`npm run lint` / `npm run build` / `vercel --prod` 成功、本番 `https://v0-line-blush.vercel.app` へ反映済み。
- 掲示板がスマホ側で再び横縞表示になる報告を受け、投稿一覧のカード配置をflex依存から撤退。`.board-page .post-list` を `display: block` にし、カード間隔は `.board-page .post-card + .post-card { margin-top: 12px; }` で確保。flex収縮そのものを発生させない構造へ変更した。本番 `TS(売上・新規・HAPPY！）` 掲示板で投稿22件、カード高さ453px/1222px/1311px、`.post-list` の `display:block` / `scrollHeight:17816` を確認済み。`npm run lint` / `npm run build` / `vercel --prod` 成功。

## 2026-06-09 更新
- 勤怠・給与計算機能の大規模実装に向け、`C:\作業用\勤怠\2026.06` の社労士資料を調査。従業員一覧、休憩時間、時給一覧、通勤費、勤怠集計、支給控除、賃金台帳、事業所負担保険料、事業所別支給集計を確認し、`docs/attendance-payroll-implementation-plan.md` にDB/権限/API/画面/実装フェーズ案を作成。
- 給与計算前提としてタイムレコーダーが必要なため、計画書に `gw_attendance_devices` / `gw_attendance_punches`、打刻API、従業員向け打刻画面、打刻ログから日次勤怠/月次集計を生成する実装順を追記。
- TSGに従業員向け勤怠打刻の土台を追加。`/attendance` 画面、下ナビの勤怠導線、`/api/attendance/time-clock`、`gw_attendance_punches` を追加し、勤務先Wi-Fi判定は `TSG_ATTENDANCE_ALLOWED_IPS` / `TSG_ATTENDANCE_ALLOWED_NETWORKS` の許可IPで行う。TS社内以外に道の駅など複数勤務先ネットワークをラベル付きで登録できる。
- 勤怠打刻は個人スマホ/IP判定を主方式にせず、本社・道の駅の専用端末方式へ変更。`gw_attendance_devices`、端末キー付き `/time-clock/[deviceKey]`、公開端末API `/api/time-clock/[deviceKey]`、管理者向け `/api/admin/attendance`、管理画面の勤怠タブを追加。名前スクエアボタン選択 → 出勤/退勤確認 → 打刻完了の流れにし、管理者は手動打刻追加と誤打刻無効化が可能。
- 管理画面の権限制御を拡張。ユーザー/グループ/勤怠修正はTSG管理者のみ、給与タブは佐藤正彦・佐藤ちさとの2名のみ表示する。下ナビの管理ボタンは管理者または給与権限者に表示する。
- `sql/014_attendance_terminal_devices.sql` を本番Supabaseへ適用し、本社/道の駅タイムレコーダー端末を作成。`npm run lint` / `npm run build` / `vercel --prod` 成功。本番 `https://v0-line-blush.vercel.app` で本社タイムレコーダーAPIが 200、スタッフ24名を返すことを確認。
- 勤怠タイムレコーダー運用マニュアルを作成。`components/attendance-operation-manual.tsx` を管理画面の「マニュアル」タブに追加し、印刷CSSでマニュアル本文のみA4印刷できるようにした。ローカル印刷用HTML `docs/tsg-attendance-operation-manual.html` と、実画面スクショ `public/manual/screenshots/time-clock-staff-list.png` / `time-clock-confirm.png` を作成。`npm run lint` / `npm run build` / `vercel --prod` 成功、本番スクショURL 200確認済み。
- `/manual` 配下を公開静的ファイルとして許可し、マニュアル内スクショ2枚が本番で `image/png` として返ることを確認。
- PC幅の掲示板/Chatで下入力ウィンドウだけ画面左右端まで伸び、メニュー/本文幅と揃わない問題を修正。`app/globals.css` に1024px以上かつfine pointer向けの上書きを追加し、掲示板/Chatの検索・本文・入力フッターを1040px幅へ統一。スマホの固定フッター/スクロール対策は変更なし。`npm run lint` / `npm run build` / `vercel --prod` 成功、本番CSS配信とChrome上のChat入力フッター1040pxを確認済み。
- TSGカレンダーのGoogle取込を手動ボタン方式から自動同期へ変更。`/api/calendar/events` が表示範囲取得前にGoogleカレンダーを同期し、カレンダー表示中も10分ごとに再取得する。同期履歴テーブル `gw_calendar_sync_status` を追加し、同じ表示範囲の短時間再同期をスキップ。手動POST APIは管理者向け強制同期として残す。`sql/015_calendar_google_auto_sync.sql` を本番Supabaseへ適用し、`npm run lint` / `npm run build` / `vercel --prod` 成功。本番 `https://v0-line-blush.vercel.app` は deployment `dpl_7J4MdBebr2wTYo3uEp5kqhB8xfqf` を返すことを確認。

## 2026-06-12 更新
- DocScanner営業一斉送信後の共有投稿用に `POST /api/integrations/doc-scanner/sales-broadcast` を追加。TSGくんとして `NEWブランド館（フロア）` 掲示板へ、営業FAX一斉送信の提案商品名・送信先一覧・サンプル依頼/問い合わせ対応依頼を投稿する。`sourceKey` で重複投稿を防止する。
- 確認: `npx eslint app/api/integrations/doc-scanner/sales-broadcast/route.ts --quiet` 成功、`npm run build` 成功、`vercel --prod --yes` 成功。本番 `https://v0-line-blush.vercel.app/api/integrations/doc-scanner/sales-broadcast` のGETで対象掲示板とTSGくんを取得できることを確認。
- 勤怠・給与機能完成に向けて `C:\作業用\労務` を再精査。従業員一覧、休憩ルール、時給、通勤費、勤怠集計、支給控除、賃金台帳、会社負担保険料、事業所別支給額を確認し、`docs/attendance-payroll-implementation-plan.md` に2026-06-12再精査メモを追記。既存打刻ログが `gw_users` 直結かつ `ON DELETE CASCADE` のため、給与用従業員マスタを別建てして監査履歴を消さない設計にする方針。
- 勤怠・給与Phase 1として `sql/016_payroll_labor_base.sql` / `supabase/migrations/202606120001_payroll_labor_base.sql` を追加し、本番Supabaseへ適用。給与従業員、事業所、原本文書/抽出行、勤怠期間、給与期間、日次/月次勤怠、通勤、給与項目、給与結果、チェックリスト、事業所別支給集計などの受け皿を作成。`gw_attendance_punches.user_id` は `ON DELETE SET NULL` へ変更し、`employee_id` / `workplace_id` を追加。
- `scripts/import_labor_data.py` を追加し、`C:\作業用\労務` の初回取込を実行。取込結果は原本文書15件、抽出行200件、給与従業員32件（社員コードあり21件、TSG紐付け24件、両方あり18件）、通勤ルート23件、月次通勤17件、給与チェックリスト9件、事業所別支給集計2件。画像PDF3件は原票として `image_only` 管理。
- 管理画面の給与タブに、取り込んだ給与・人員マスタ一覧を追加。`GET /api/admin/payroll/employees` は給与管理権限者のみ利用可能で、従業員数、社員コードあり、TSG紐付け、コード+紐付け件数を表示する。`npm run build` 成功、`vercel --prod --yes` 成功。本番 `https://v0-line-blush.vercel.app/admin` に反映済み。
- `scripts/import_labor_data.py` を過去分一括取込向けに拡張。`--batch-root` 配下の `2026.01` / `R08.01` 形式の月別フォルダを検出し、`--months 6` で直近6ヶ月分の確認JSON/投入SQL/manifestを生成できる。支給月はフォルダ名、勤怠月は前月、支給日は10日で自動設定。`C:\作業用\勤怠` の1ヶ月分でbatch dry-run成功。

## 2026-06-12 追記
- `C:\作業用\労務` の2025.08〜2026.06通常給与11ヶ月分と2025.12賞与を一括取込。`gw_payroll_periods.payroll_kind` を追加し、通常給与と賞与を同月で分離できるようにした。
- PDFの支給控除一覧/賞与一覧から人別の支給合計・控除合計・差引支給額を `gw_payroll_employee_results` に保存。2025.08、2026.01、2026.05は10日固定ではなくPDF上の実支給日（2025-08-08、2026-01-09、2026-05-08）を採用。
- 管理画面の給与タブに人件費分析を追加。月別、組織別、人別、明細別に支給合計/控除/差引を見られる。APIは `GET /api/admin/payroll/labor-costs`。
- 確認: `npx eslint app/admin/page.tsx app/api/admin/payroll/employees/route.ts app/api/admin/payroll/labor-costs/route.ts --quiet`、`npm run build`、`vercel --prod --yes` 成功。本番 `https://v0-line-blush.vercel.app` へ反映済み。
- 管理画面の給与・人件費UIをPC前提に調整。`.admin-page` がPCでもスマホ幅600pxに制限される上書きを解除し、960px以上のfine pointerでは最大1368〜1720pxの作業幅、給与表は2カラム表示にした。CSS先頭BOMで全体リセットが効かないケースに備え、resetを明示的に再定義。
- 確認: PC幅1440pxのCSSプレビューでbody余白0、管理ページ幅1368px、横スクロールなしを確認。`npx eslint app/admin/page.tsx app/globals.css --quiet`、`npm run build`、`vercel --prod --yes` 成功。本番 `https://v0-line-blush.vercel.app` へ反映済み。
- 給与タブのUIを再整理。PC前提で、左カラムに対象期間・給与人員マスタ・組織別、右カラムに月別推移・人別ランキング・明細を配置し、表は表内スクロール、人員リストは縦スクロールにした。
- 給与人員マスタで在籍/退職/停止とTSG連携先を設定できるように `PATCH /api/admin/payroll/employees` を追加。未連携は退職者として扱う表示にし、復帰・新規連携は同画面で選択する。
- 確認: PC幅1440pxのCSSプレビューで左420px/右866px、従業員カード154px、ページ横暴れなしを確認。`npx eslint app/admin/page.tsx app/api/admin/payroll/employees/route.ts app/globals.css --quiet`、`npm run build`、`vercel --prod --yes` 成功。本番 `https://v0-line-blush.vercel.app` へ反映済み。
- 既存DBの未連携従業員を一括で退職ステータスへ寄せる処理は、ローカル/Vercel pullで取得できるSupabase接続値が空だったため未実行。画面上では個別に退職/連携設定可能。
- 給与計算と人事管理を同じ管理内の別UIとして分離。給与計算タブは支払済み給与・賞与の月別/組織別/人別集計、人事管理タブは在籍/退職/TSG連携/住所電話などの人事プロフィール管理、労務データタブは社労士事務所から取り込んだ原文ファイルと抽出状況の一覧に整理。
- 退職者削除は給与従業員行の物理削除ではなく、人事プロフィールの削除マークと連絡先クリアで実装。過去の `gw_payroll_employee_results` は残るため、退職者を人事画面から消しても支払済み給与履歴は保持される。
- 追加APIは `GET/PATCH/DELETE /api/admin/hr/employees` と `GET /api/admin/labor/imports`。権限は給与閲覧権限（佐藤正彦/佐藤ちさと想定）に合わせた。
- 確認: `npx eslint app/admin/page.tsx app/api/admin/hr/employees/route.ts app/api/admin/labor/imports/route.ts app/api/admin/payroll/labor-costs/route.ts app/globals.css --quiet` 成功、`npm run build` 成功、PC幅プレビューでページ全体の横スクロールなしを確認、`vercel --prod --yes` で本番反映済み。
- 人事管理一覧の所属表示をTSG部署3種（フロア/製造/道の駅）へ正規化。並び順は本人を最上段、その下をフロア→製造→道の駅、同部署内は入社日順に変更。ユーザー管理一覧も本人最上段＋部署順へ寄せた。給与計算の組織集計も勤務先名ではなくTSG部署3種へ統一。
- 勤怠管理のタイムレコーダー端末カードに端末URLのQRコードを追加。QRをクリックすると該当 `/time-clock/[deviceKey]` を開く。現状のタイムレコーダー人員表示は承認済みTSGユーザー全員の部署別表示で、端末/勤務場所ごとの表示制御UIは未実装。
- タイムレコーダー端末別の人員表示を分離。本社端末はフロア/製造のみ、道の駅端末は道の駅のみを返すよう `/api/time-clock/[deviceKey]` を変更し、POST打刻時も対象外スタッフを403で拒否する。打刻確認画面は出勤/退勤ボタンとカードを小型化し、打刻成功後は即名前一覧へ戻るよう変更。スマホ幅390pxで戻るボタンが画面内に収まることを確認。
- 本番DB `zfhswguzqyagmhhlpksq` がTSG本番であることを、TSGグループ、本社/道の駅タイムレコーダー、給与従業員45件、給与結果234件、労務取込14件で読み取り確認。TSG未連携の給与従業員21件を退職者として一括処理し、`payroll_status=retired`、`resigned_date=2026-06-12`、`raw_payload.hr_profile.deleted_at` 付きの人事削除済みに変更。給与結果234件は変更なし。更新前データは `backups/gw_payroll_employees_unlinked_before_retire_2026-06-12T07-05-02-631Z.json` に保存。
- ホーム上部に「本日出勤」エリアを追加。`gw_attendance_punches` の当日最終打刻が出勤のユーザーだけを丸アイコンで横並び表示し、退勤が最後になった人は一覧から消えるようにした。390px幅で横スクロールなしを確認。
- PC版ホームで `.groups-list` が共通600px幅に縛られ、内部スクロールバーが画面中央に出る崩れを修正。スクロールコンテナは画面幅いっぱい、カードと本日出勤枠だけを760px中央寄せにし、951px/390px幅で横スクロールなしを確認。
- 本日出勤の丸アイコン表示を横スクロールから折り返し表示へ変更。同エリアを押すと詳細を開閉し、出勤中スタッフの名前・所属・出勤時刻を縦リストで確認できるようにした。
- ホーム表示時に内部スクロール位置が途中復元され、本日出勤パネルが細いバー状に見切れる問題へ対応。`groups-list` を初回表示で先頭へ戻し、本日出勤パネルに最低高さを持たせてアイコン行が潰れないよう固定。
- DocScannerからの `FAX受信` 自動投稿で、ダイサン食材FAXの添付画像が上下逆だった既存投稿を同じ受信IDで更新し、補正済み画像へ差し替え。原因対応はDocScanner側の受信プレビュー生成で実施。
- 2026-06-12 勤怠管理に打刻修正/打刻忘れ対応を追加。`gw_attendance_punches` に自家用車距離、休憩上書き、申請者/時刻の列を追加し、管理画面では既存打刻の編集・無効化・手動追加時の距離/30分休憩指定を可能にした。タイムレコーダー退勤時は自家用車km、備考、30分休憩ボタンを入力できる。
- 2026-06-12 管理画面をPCでもスマホ幅レイアウトへ戻し、タブ/給与/人事/労務/勤怠フォームが1カラム基準で収まるようCSSを再調整。LINEログイン時のプロフィール画像URLに同期時刻を付けて保存し、スタッフがLINEアイコンを変更した後の再ログインで古い画像キャッシュを掴まないようにした。
- 2026-06-12 ログイン済みスタッフ用 `/attendance` の退勤時にも自家用車距離、備考、30分休憩ボタンを追加し、`/api/attendance/time-clock` から勤怠打刻へ保存するよう変更。専用端末 `/time-clock/[deviceKey]` も退勤ボタンの前に同入力欄を表示する順番へ調整。
- 2026-06-12 自家用車入力を「場所（テキスト）」と「距離（数字km）」に分離し、スタッフ用/専用端末/管理修正APIで `private_vehicle_place` と `private_vehicle_distance_km` を保存するよう変更。タイムレコーダーの名前ボタンはフロア=青、製造=緑、道の駅=濃い黄に色分けし、`system_tsg_` のTSG君は端末スタッフ一覧から除外。
- 2026-06-12 DocScanner EC速報連携APIに `action=zero-posts` を追加。TSGくんがEC速報掲示板へ投稿した全0速報だけを連携secret付きで列挙できるようにした。全0投稿5件を削除し、DocScanner実データから2026-06-03/2026-06-13の修正版を投稿済み。`npx eslint app/api/integrations/doc-scanner/ec-report/route.ts --quiet` / `npm run build` / `vercel --prod --yes` 成功。
- 2026-06-13 管理画面の人事管理を名前だけの折りたたみ一覧へ変更。詳細を開いた時だけTSG連携、退職処理、住所/電話などの人事情報を表示し、給与従業員マスタに `work_style` を追加して「5日正社員」「6日正社員」「パート」「フルタイムパート」「役員」をボタンで保存できるようにした。本番Supabaseへ `sql/019_payroll_employee_work_style.sql` 適用済み。
- 2026-06-13 人事管理の折りたたみ行で、スタッフ名の横に社員NO、勤務形態、所属をアイコン付きチップで表示するよう調整。詳細を開かなくても基本情報を判別できるようにした。
- 2026-06-13 タイムレコーダー端末のスタッフ表示順を、部署ごとに給与従業員マスタの入社日が古い順へ変更。入社日未設定者は最後、同日または未設定同士は名前順にした。
- 2026-06-13 人事管理の詳細欄に社員NOと入社日を編集できる基本情報欄を追加。`PATCH /api/admin/hr/employees` で `employee_code` / `hire_date` を保存できるようにし、社員NO重複時はエラーを返すようにした。
- 2026-06-13 過去労務データの丸め単位を確認。`C:\作業用\労務` の2025.08〜2026.06 `従業員毎勤怠集計.pdf` すべてに `:15` / `:45` の勤務時間が多数あり、2026.06支給控除一覧でも `91:15 × 1,100円 = 100,375円` など給与額と一致するため、社労士運用は15分単位と判断。TSG側の暫定DB設定 `rounding_unit_minutes=30` は給与計算実装前に15へ修正が必要。
- 2026-06-13 上記実態に合わせ、勤怠/給与の丸め設定を15分単位へ修正。本番Supabaseの `gw_attendance_periods` 既存11件、`gw_attendance_rounding_rules.default`、各テーブルのデフォルト値をすべて15へ更新し、取込スクリプトと初期/差分SQLも15分基準に統一した。30分休憩ボタンは休憩控除用の別機能として維持。
- 2026-06-13 正社員系の過去給与計算を藤田香織/武藤志保で確認。両名とも基本給は固定で、平日土曜残業は `基本給 ÷ 月間除数 × 1.25`、日曜残業は `基本給 ÷ 月間除数 × 1.35` の円単位丸めで加算されている。藤田香織は月間除数192、武藤志保は月間除数168で計算されており、現行TSGの勤務形態だけから除数を一律判定しないよう給与プロファイル側に除数を持つ必要あり。
- 2026-06-13 武藤志保の勤怠実績を確認。従業員毎勤怠集計では全月 `所定 8:30-16:30`、欠勤0日、実出勤は21〜24日、暦日ベースの休みは6〜8日。標準労働は6.5h/日想定だが、給与計算上の割増除数は168h/月で固定されているため、所定時間・休憩・割増除数を別項目として管理する必要あり。
- 2026-06-13 武藤志保の「月6日休み＋有給」仮説を確認。従業員毎勤怠集計の空白日は6～8日で、月6日公休を基準にすると公休超過分は0～2日。欠勤は全月0日のため、超過分は有給または有給相当の非控除休みとして扱われている可能性が高い。毎月固定2日有給ではなく、月ごとの差分として管理する。
- 2026-06-13 労務士給与データ2025.08〜2026.06を座標ベースで再解析し、社員別の給与計算プロフィール（時給/月給固定/月給+残業/役員固定、基本給・時給・残業除数・控除スナップショット）を `gw_payroll_calculation_profiles` に投入。TSG給与計算タブへ計算ロジック一覧と今月打刻ベース試算を追加し、`npm run lint` / `npm run build` / `vercel --prod --yes` 成功。本番 `https://v0-line-blush.vercel.app` へ反映済み。
## 2026-06-13 更新
- DocScanner連携 `/api/integrations/doc-scanner/sales-broadcast` が `type: press_release` / `press-release:*` の通知IDを受けた場合、営業一括とは別文面で「プレスリリース一斉送信」としてTSG掲示板へ投稿するようにした。
- 投稿内容は件名、送信日時、成功/失敗件数、対象商品、送信先一覧、取材依頼・掲載問い合わせの対応依頼。既存の営業一括配信投稿ロジックは維持。
- 確認: 対象ファイルの `npx eslint --quiet` 成功、TSG `npm run build` 成功。`vercel --prod --yes` で本番 `https://v0-line-blush.vercel.app` に反映済み。
- 2026-06-14 勤怠管理の打刻ログを期間・スタッフ・端末で絞り込めるようにし、テスト打刻の一括削除（監査ログ付き無効化）と削除済み表示切替を追加。日数/人数が増えてもログ枠内で日付別にスクロールできるUIへ整理。
- 2026-06-15 勤怠管理の打刻ログに「今日」「今月」「先月」の期間ショートカットを追加。押下時に開始日/終了日を同期し、その範囲で即表示するようにした。
- 2026-06-15 道の駅タイムレコーダーの退勤確認では、場所・距離・30分休憩ボタンを非表示にし、備考だけ入力できるように変更。API側でも道の駅端末からの自家用車/休憩上書き値は保存しないようにした。
- 2026-06-15 DMでも自分が送信したメッセージを削除できるように変更。DMでは相手の投稿は削除不可のままにし、グループChatの管理者削除は維持。
- 2026-06-15 掲示板/コメント/グループChat/DMのファイル添付をドラッグ&ドロップ対応に変更。画面全体に落とすと投稿/メッセージ添付、コメント欄に落とすとコメント添付になり、ドラッグ中はドロップ範囲を表示する。

## 2026-06-18 グループChat日時表示
- グループChatの投稿時刻表示を時刻のみから `YYYY/MM/DD HH:mm` へ変更。スマホで途中改行されないよう、投稿日時表示に `white-space: nowrap` を追加。

## 2026-06-18 セッション再調整
- TSGログインセッション期限を7日固定から30日固定へ変更。ログイン時刻をCookieに含める方式は維持し、30日経過後に再ログインする。
- 通知クリックでセッションが無い場合は /login?next=通知先 に退避し、LINE再ログイン後に元の掲示板/Chatへ戻す。Service Workerの通知クリック処理はログイン画面だけを再利用しないよう修正。
- アプリ起動時に /sw.js の登録/更新確認を行い、通知クリック修正版が端末へ反映されやすいようにした。

## 2026-06-18 PWAセッション修正
- `gw_user_session` を新形式だけに限定した変更を撤回し、旧形式のユーザーID Cookieも有効扱いに戻した。新規ログインCookieも以前と同じユーザーID形式へ戻し、ホーム画面PWA側の既存ログインを無効化しないよう修正。

## 2026-06-18 初回DM修正
- 初めてDMする相手でもチャット画面へ入れるよう、DM作成APIで既存/新規どちらも `gw_group_members` をupsertしてから返すよう修正。
- `/api/chat` 側でも `direct:` グループの説明に本人IDがある場合、欠けたDMメンバー行を自動復旧して403にならないようにした。DM一覧からの遷移はフルリロードではなくNextルーター遷移へ変更。

## 2026-06-18 アイコン統一
- Safari/PWA、Chrome、favicon、通知のアイコンを同じ `TSG` デザインへ統一。`favicon.png`、`apple-touch-icon.png`、`apple-icon.png`、`icon-192.png`、`icon-512.png`、`icon.svg` を再生成し、manifest/HTML/Service Workerの参照に `v=20260618-tsg` を付けて古いアイコンキャッシュを避ける。

## 2026-06-19 道の駅タイムレコーダーSOS
- 道の駅タイムレコーダーだけに小型のSOSボタンを追加。押下時は `/api/time-clock/[deviceKey]/sos` から承認済み管理者（`role=admin`）へ「道の駅タイムレコーダーでSOSが押されました。」のPush通知を送る。

## 2026-06-19 TSGくんアイコン
- 共通アバター部品で `TSG君` / `TSGくん` を検出し、LINE画像より優先してAIロボット風の顔アイコンを表示するように変更。掲示板、Chat、DM、メンバー一覧などSafeLineAvatar利用箇所に反映。

## 2026-06-19 TSGくんアイコン再調整
- TSGくん専用アバターが掲示板ヘッダー内でflex伸長して大きな青い玉になる問題を修正。サイズをインライン/ CSS 双方で固定し、SVGではなくCSS顔パーツでAIロボット顔を表示する方式へ変更。

## 2026-06-21 勤怠月末修正・労務士提出
- 管理 > 勤怠に月末勤怠修正パネルを追加。対象月と所属（フロア/製造/道の駅）を選び、スタッフ別に1か月分の出勤/退勤、30分休憩、労務士向け備考をまとめて確認・保存できるようにした。
- `PATCH /api/admin/attendance` に月次1日分更新アクションを追加。打刻忘れは追加、誤打刻は更新、空欄化は無効化として監査ログへ残す。
- `GET /api/admin/attendance/export` を追加。所属メンバーをスタッフ別シートに分けたExcel形式（.xls）で出力し、月末に労務士提出データとしてダウンロードできるようにした。
- 月末勤怠修正の1日分入力をカード型から1行型へ変更。表示項目を日付・出勤・退勤・備考に絞り、1か月分を縦に素早く編集できるようにした。

## 2026-06-24 掲示板過去投稿読み込み
- 掲示板投稿APIに `before` カーソルと `hasMore` を追加し、50件を超える親投稿を古い順へ追加取得できるようにした。
- 掲示板画面下に「過去投稿を読み込む」ボタンを追加。初回50件の後、さらに古い投稿を重複除外しながら追加入力する。

## 2026-06-24 掲示板投稿固定
- 掲示板の親投稿を管理者がTOP固定/固定解除できるようにした。コメントは固定対象外。
- 固定投稿は作成日時に関係なく初回表示でTOPに出し、通常投稿はその下で新しい順に表示する。過去投稿追加読み込み時も固定投稿のTOP表示を維持する。

## 2026-06-24 勤怠月次修正の日別削除
- 管理 > 勤怠 > 月末勤怠修正の各日行にゴミ箱ボタンを追加。選択スタッフの該当日付の有効打刻をまとめて削除扱い（無効化）できるようにした。
- `PATCH /api/admin/attendance` に `monthly_day_delete` を追加。給与計算対象から外しつつ、監査ログと無効化理由は残す。

## 2026-06-24 メンション通知文面
- メンションPush通知のタイトルに発生場所を入れるよう変更。掲示板は `掲示板「グループ名」でメンション`、Chatは `Chat「グループ名」でメンション`、DMは `DM「相手/グループ名」でメンション` の形式。
- 通知本文は `送信者名: 内容抜粋` とし、どの場所で誰から呼ばれたか通知だけで分かるようにした。

## 2026-06-24 タスク内メンション履歴
- `gw_mentions` テーブルを追加し、メンションPush通知送信時にメンション履歴を保存するようにした。保存対象はメンションされたユーザー、送信者、掲示板/Chat/DM種別、グループ名、投稿URL、内容抜粋。
- タスク一覧に「自分へのメンション」セクションを追加。直近30件を表示し、クリックで該当掲示板/Chat/DMを開けるようにした。
- メンション履歴は保存時とタスク一覧取得時にユーザーごと直近30件を超えた分を自動削除する。

## 2026-06-24 社長メンション名
- 掲示板/コメント/Chatのメンション候補で、佐藤正彦さんは `社長` と表示・挿入するようにした。
- 投稿/Chat保存時に `@佐藤正彦さん` / `@佐藤 正彦` / `@社長さん` などを `@社長` へ正規化し、通知・履歴の検出側も `@社長` で本人に届くようにした。

## 2026-06-27 固定投稿の折りたたみ
- 掲示板の固定投稿は初期表示で本文を2行に折りたたみ、`全文を表示` または本文プレビュークリックで全体表示するようにした。
- 未展開の固定投稿ではOGPプレビューと添付表示を隠し、展開後だけ通常投稿と同じ全表示にする。通常投稿とコメント表示は従来通り。

## 2026-06-28 6月前半PDF勤怠取込
- 添付PDF（製造/フロア/道の駅スタッフ）から2026-06-01〜2026-06-15の打刻を本番Supabase `gw_attendance_punches` へ `source_type=import` で348行投入。6/16運用開始前の端末テスト打刻は有効集計から外すため無効化した。
- PDF名と現行TSG名が異なるものはメモを残して現行連携レコードへ寄せた。`内海美穂 -> 生井美穂`、`鈴木結芽香 -> 森結芽香`、佐藤葵はフロア/製造の現行レコードへ紐付け。
- 生井美穂、佐藤 葵（フロア）、佐藤葵（製造）は過去給与プロファイルが退職者側レコードに残っていたため、現行TSG連携レコードへ給与プロファイルをコピーした。
- PDF上で片側のみだった打刻はメモ付きで反映。未確定は松崎正恵さん2026-06-09（出勤欄16:09、退勤手書き16:00のため出勤時刻未取込）と渡部瞳さんの出勤欄空欄5件。
- 勤怠備考欄は労務士へ伝える必要がある内容だけに整理。取込元PDF名・ページ・PDF名差分・「6時間以上で休憩あり」などのメタ情報は削除し、通常行は空欄、例外は休憩なし/残業分/打刻不整合/押し忘れのみ残す。小島遼さんの休憩なしは退勤行に `break_override_minutes=0` を反映。

## 2026-06-29 シフト機能要件整理
- `C:\Users\ts\Downloads\シフト表.xlsx` を確認し、TSG勤怠へ追加するシフト機能の要件定義を `docs/shift-management-requirements.md` に作成。フロア/製造/道の駅の部門別作成、15日単位/任意期間、希望休回収、AI下書き、人間修正、Excel出力、勤怠/給与連携を整理した。

## 2026-06-29 シフト機能MVP実装
- シフト専用テーブル（期間、勤務パターン、日別条件、希望、割当、生成履歴）を追加し、本番Supabase `zfhswguzqyagmhhlpksq` へ反映。初期勤務パターンは既存シフト表からフロア/製造/道の駅別に投入した。
- 管理 > シフトを追加。所属別に期間作成、希望確認、AI下書き、手修正、確定、Excel出力ができるようにした。スタッフ候補はTSG連携済み・在籍・人事削除なしの給与従業員から所属別に取得する。
- スタッフ用 `/shifts` を追加。下メニューから開き、自分の所属のシフト期間に対して休み希望・出勤不可・時間希望・メモを提出できる。打刻機能とは分離し、専用端末タイムレコーダー運用には影響しない。
- `npm run build` とVercel本番デプロイを確認。`v0-line-blush.vercel.app` へエイリアス済み。

## 2026-06-29 シフト期間ルール調整
- 管理 > シフトの新規作成初期値を、基本運用に合わせて「前半: 毎月1日〜15日」「後半: 16日〜月末」に変更。
- 前半/後半をワンタップでセットできるボタンを追加。タイトル未入力時は `フロア 2026年6月前半` のように所属・年月・前後半で自動命名する。
- `npm run build` とVercel本番デプロイを確認。`v0-line-blush.vercel.app` へエイリアス済み。

## 2026-06-29 シフト管理UI整理
- 管理 > シフト上部のUIを「開くシフト」と「シフト期間を作る」に分離。既存期間の選択と新規作成が同じ列に混ざって分かりにくかった状態を整理した。
- PC幅ではシフトタブだけ管理画面の横幅を広く使うよう調整。新規作成フォームはシフト名を1行、開始日/終了日/希望締切/作成を次行に分け、日付入力が `2026/0` のように潰れないようにした。
- `npm run build` とVercel本番デプロイを確認。Chromeで本番のシフトタブ表示を確認済み。

## 2026-06-29 シフト勤務パターン候補調整
- 管理 > シフト表の勤務候補をスタッフの勤務形態で出し分け。5日/6日正社員は基本勤務系を表示し、パート/フルタイムパートは時間帯パターンだけを候補表示するように変更。
- API保存時にも、パート/フルタイムパートへ `基本勤務` / `基本勤務＋...` / `フロア勤務` が入らないようチェックを追加。AI下書きもスタッフごとに正社員は基本勤務、パートは時間帯パターンを使う。
- 既にAI下書きでパートへ基本勤務が入っていた本番データ50件を時間帯パターンへ補正し、残件0件を確認。Vercel本番デプロイ済み。

## 2026-06-29 シフト表スマホUI
- 管理 > シフト表はスマホで横長Excel表を表示せず、日付カードごとにスタッフを縦並びで編集するUIへ切り替え。横スクロールバーが画面外へ逃げて隣のスタッフが見えない問題を解消。
- PCでは従来の表形式を維持し、スマホでは日付・必要人数・場所・Timee・備考・スタッフ別勤務を1画面幅で確認/編集できるようにした。
- `npm run build` とVercel本番デプロイを確認。`v0-line-blush.vercel.app` へ反映済み。

## 2026-06-29 シフト修正保存・削除操作
- 管理 > シフト表のセル保存を、React状態ではなく入力欄の現在値を直接APIへ渡す方式に変更。入力直後のblur保存で古い値が送られ、修正できないように見える問題を修正。
- 必要人数、場所、Timee、備考、作業予定、スタッフ別勤務の保存も同じ方式へ統一。
- 選択中シフトに `割当全削除` と `期間削除` を追加。割当全削除は希望・必要人数・備考を残してスタッフ割当だけ削除し、期間削除はシフト期間ごと削除する。どちらも確認ダイアログ付き。
- `npm run build` とVercel本番デプロイを確認。`v0-line-blush.vercel.app` へ反映済み。

## 2026-06-29 シフト表PC表示・早遅バランス
- 管理 > シフト表のPC表示を調整。フロアは横スクロールなしで1画面に収め、製造はスタッフ数が多い場合に2画面切替で表示するようにした。
- シフト表内の縦スクロールバーを廃止し、ページ全体のスクロールで見る形へ統一。備考列も圧縮して表の視認性を改善した。
- フロアのAI下書きはパート/フルタイムパートの早番・遅番が日別/人別で偏りにくいように割当ロジックを調整。既存AI割当52件も早番/遅番へ再配分した。
- `npm run build` とVercel本番デプロイを確認。`v0-line-blush.vercel.app` へ反映済み。

## 2026-06-29 シフト名自動生成・一覧整理
- シフト名は入力式を廃止し、`所属 YYYY-MM-DD〜YYYY-MM-DD` 形式で自動生成するように統一。管理側とスタッフ側の表示名も同じルールへ揃えた。
- 管理 > シフトに作成済みシフト一覧を追加。一覧から対象シフトを開く/削除できるようにした。
- 新規作成直後は `下書き` とし、管理者が `希望回収` を押した時点でスタッフ側 `/shifts` に表示される運用へ変更。公開前の下書き期間への希望提出はAPIでも拒否する。
- `npm run build` とVercel本番デプロイを確認。`v0-line-blush.vercel.app` へ反映済み。

## 2026-06-29 シフト管理PC全幅化
- PC表示時の管理 > シフトだけ、管理ページとシフト管理コンテナの横幅上限を外し、画面幅を広く使うようにした。管理タブも同じ幅に揃えた。
- 本番画面で1920px幅時にシフト管理コンテナが約1881pxまで広がることを確認。フロア `2026-07-01〜2026-07-15` は `希望回収中` のためスタッフ側に表示される状態。
- `npm run build` とVercel本番デプロイを確認。`v0-line-blush.vercel.app` へ反映済み。

## 2026-06-29 シフト希望回収導線
- 希望回収は管理者が対象スタッフを選択して開始し、対象スタッフのホーム上部に回収カードを表示する方式へ変更。カードから `/shifts?period_id=...` を開き、該当期間のカレンダーで希望を提出する。
- 管理 > シフトに対象者選択、提出済み/未提出/未送信、希望件数の一覧を追加。シフト表ヘッダーにも回収状況バッジを表示し、休み希望/出勤不可の日は黒/グレー表示で割当不可にした。AI下書きも希望休をスキップする。
- `希望全削除` は提出データだけでなく回収対象も解除し、スタッフのホーム表示を消す動作へ変更。スタッフ側の希望提出は回収中の期間だけ許可する。
- `npm run build` とVercel本番デプロイを確認。Chrome本番確認ではホーム上部のシフト希望カード0件、管理 > シフトも作成済みシフトなしを確認。

## 2026-06-29 シフト希望UI簡略化
- 下メニューのシフトボタンを廃止し、ホーム上部のカレンダー横に丸型のシフトショートカットを追加した。
- スタッフ側 `/shifts` の希望提出UIを、出られない日だけをカレンダー上で黒塗り選択する方式に変更。セレクト、時間希望、メモ入力はスタッフ画面から外し、保存時も `unavailable` のみを送る。
- 本番Chromeでホーム上部にカレンダー/シフトの丸アイコンが並ぶこと、下メニューからシフトが消えていること、シフト画面に15日分の1画面カレンダーが表示されることを確認。Vercel本番デプロイ済み。

## 2026-06-29 シフト管理操作パネル整理
- 管理 > シフトのサマリー/操作パネルが横長画面で間延びし、ボタンが縦積みになる状態を整理。
- 日数/スタッフ/対象/提出/希望/割当をカード状のサマリーにし、通常操作（希望回収、スタッフ画面、AI下書き、調整中、確定、Excel出力）と削除系（割当全削除、希望全削除、期間削除）を別グループの横並びツールバーに分けた。
- 補足説明は横に流さず下段の控えめな情報ボックスに変更。`npm run build` とVercel本番デプロイ済み。

## 2026-06-29 道の駅シフト時間帯・個人傾向反映
- `シフト表.xlsx` の道の駅ブロックを集計し、実績パターン（基本勤務、8:30-16:00、11:00-14:00、11:00-15:00、11:00-15:30、10:00-15:00、10:30-14:00）と個人別頻度をDBへ登録。`生井美穂` / `内海美穂` は同一傾向として扱う。
- 管理 > シフト表の勤務欄を `datalist` から通常のプルダウンへ変更し、現在値に候補が絞られて1件しか見えない問題を解消。
- AI下書きは道の駅などフロア以外で `gw_shift_pattern_preferences` を参照し、個人ごとの多い時間帯を重み付きで反映するように変更。既存の道の駅 2026-07-01〜2026-07-15 のAI割当も手動行を除いて傾向に合わせて補正した。
- `npm run build` とVercel本番デプロイ、リモートDBマイグレーション適用、本番Chromeで道の駅候補表示を確認済み。

## 2026-06-30 製造シフト作成安定化
- 管理 > シフトで部署切替直後の古い読み込み結果が後から戻り、部署状態を上書きする可能性を防ぐため、シフト読み込みに連番ガードを追加。
- シフト期間作成後の再読み込みで失敗した場合も作成ボタンが固まらないよう、作成処理を `try/catch/finally` に整理。読み込み中は作成ボタンを無効化した。
- 6/30時点の新規作成初期値が過去の6月後半になる問題を修正し、次の作成対象（2026-07-01〜2026-07-15）を初期表示するよう変更。
- 本番Chromeで製造タブが `製造 2026-07-01〜2026-07-15`、作成ボタン有効の状態になることを確認。`npm run build` とVercel本番デプロイ済み。
## 2026-06-30 勤怠30分休憩メモ反映
- タイムレコーダー退勤時に30分休憩ボタンを押した場合、備考の先頭へ `30分休憩` を自動付与するよう変更。既存の30分休憩フラグ付き打刻も、管理画面APIの表示時に備考へ補完する。
- 管理者の打刻追加・月次修正・個別編集でも30分休憩指定時に備考先頭へ反映し、二重付与しないようガードを追加。
- 労務士提出Excelの備考生成も同じルールに合わせ、30分休憩と手入力備考が重複しないよう整理。`npm run build` 済み。
## 2026-06-30 勤怠月次修正スマホUI調整
- 管理 > 勤怠 > 月次勤怠修正のスマホ表示で、備考列と削除ボタンが重なって見える問題を修正。
- スマホでは日別行を「日付 / 出勤 / 退勤 / 削除」の1行にし、備考入力は下段全幅に配置するようCSSを調整。`npm run build` 済み。
## 2026-06-30 勤怠30分休憩マーク化
- 30分休憩指定を備考欄の文字ではなく、月次勤怠修正の独立した `30休` マークとして表示するよう変更。
- 管理者が `30休` マークを押してオン/オフし、既存の「変更をまとめて保存」で `break_override_minutes` を修正できるようにした。
- 前回反映分で備考先頭へ入った `30分休憩` は、管理画面表示・修正保存・労務士提出Excel出力では備考から除外し、専用フラグ側だけに集約する。`npm run build` 済み。
## 2026-07-01 労務士提出Excelの拘束時間列追加
- 勤怠提出Excelで休憩判定の根拠が見えるよう、`出勤 / 退勤 / 拘束時間 / 休憩時間 / 勤務時間` の構成に変更。
- `拘束時間` は出勤から退勤までを15分丸めした時間、`勤務時間` は休憩控除後の時間として出力する。計算ロジック・DBは変更なし。`npm run build` 済み。
## 2026-07-01 月次勤怠修正画面の時間内訳表示
- 管理 > 勤怠 > 月次勤怠修正の各日付行に `拘束 / 休憩 / 勤務` を追加し、出勤・退勤・30休マークから即時計算して表示するよう変更。
- PCは横長の一覧で根拠時間を確認できる構成、スマホは入力行・時間内訳行・備考行に分けて潰れないよう調整。計算ロジック・DBは変更なし。`npm run build` 済み。
## 2026-07-01 勤怠月次修正画面の横スクロール廃止
- 管理 > 勤怠の月次勤怠修正で、PC表示時は勤怠管理ページ自体の横幅制限を外し、日別行の最低幅固定と横スクロールを廃止。出勤・退勤・拘束・休憩・勤務・30休・備考・削除を画面幅内に収めるようCSSを調整。
- スマホ表示は既存の2段構成を維持し、`npm run build` 済み。

## 2026-07-01 勤怠提出運用の労務士計算合わせ
- 過去の労務士計算は、拘束時間ベースではなく会社独自の勤務時間ベースで休憩を扱っていたため、当面の勤怠提出画面・提出Excelは `出勤 / 退勤 / 勤務時間 / 30分休憩 / 労務士への連絡備考` に戻す。拘束時間・休憩時間の表示列は削除。
- 拘束時間ベースの休憩算出は、将来会社システムを労基法上の正攻法へ改正する際に再検討するため、現時点では内部検討メモとして保持し、現場運用には出さない。`npm run build` 済み。

## 2026-07-01 勤怠管理PC幅の再調整
- 月次勤怠修正の項目削減後、PC表示が横に長すぎたため、管理 > 勤怠ページのPC幅を全幅から最大980pxの中央寄せへ戻した。横スクロールなしは維持。`npm run build` 済み。

## 2026-07-01 勤怠チェック完了と労務士提出
- 月末勤怠修正にスタッフ別の「チェック完了」を追加。月ごとの確認状態を保存し、全所属スタッフがチェック済みになった場合のみ労務士提出ボタンを有効化。
- 「労務士へ3ファイル送る」でフロア・製造・道の駅の勤怠ExcelをGoogle Driveへ作成し、榎田哲士社会保険労務士事務所宛のGmail作成画面を開くようにした。本文末尾に「株式会社テクニカルスタッフ AI System（TSA）より送信」を追加。`npm run build` 済み。

## 2026-07-01 月末勤怠提出UI整理
- 既存の「データ作成」が提出ボタンに見えて紛らわしかったため、「選択所属Excelを確認」に名称変更し、確認用ダウンロードであることを明示。
- 月末勤怠修正の上部に「修正して保存 → スタッフごとにチェック完了 → 労務士提出メール作成」の流れを表示。チェック完了ボタンは修正対象スタッフ欄の直下に大きく配置し、提出ボタンは「3所属Excel作成＋Gmail画面起動」であることを補足表示。`npm run build` 済み。

## 2026-07-02 勤怠提出ボタンの送信化
- 月末勤怠修正から確認用Excelダウンロード導線を削除し、全スタッフのチェック完了後に大きめの「労務士へ提出」ボタンだけが有効になる流れへ変更。
- `POST /api/admin/attendance/submit` をGmail作成画面起動ではなく、Gmail APIでフロア・製造・道の駅の3つのExcelを添付送信する実装へ変更。送信には `GOOGLE_GMAIL_REFRESH_TOKEN` が必要なため、env例と `npm run google:gmail-token` を追加。`npm run build` 済み。

## 2026-07-03 勤怠提出未着調査と予備Excel
- 労務士未着の原因調査として本番Vercel envを確認。宛先は `tatuya.enokida@gmail.com` / `next-wave.10-19@kha.biglobe.ne.jp` でコード上正しいが、`GOOGLE_GMAIL_REFRESH_TOKEN` が本番未設定のためGmail API送信は成立しない状態。
- 未設定時にDriveファイル作成まで進まず即エラーを返すよう提出APIを調整。画面側にも「予備Excelダウンロード」を追加し、フロア・製造・道の駅を個別にダウンロードして手動送付できるようにした。`npm run build` 済み。

## 2026-07-03 勤怠提出ボタンの無反応対策
- 提出ボタンが全員チェック済み前に `disabled` になり、クリックしても何も反応しない状態だったため、未チェック時もクリック可能にして不足人数をアラート表示するよう修正。
- 提出通信に失敗した場合も黙らず、労務士へ未送信であることと予備Excel送付を促すエラーを表示。`npm run build` 済み。

## 2026-07-03 勤怠チェック済みDB保存化
- 月末勤怠のチェック済み状態をブラウザ内保存から `gw_attendance_monthly_checks` テーブル保存へ変更。端末・ブラウザ・再ログイン・デプロイで戻らないよう、月別スタッフ別にDBへ永続化する。
- 6月分は「全てチェック済み」としてDBマイグレーション時に承認済みスタッフを復元。提出APIもブラウザから渡されたチェック配列ではなくDBのチェック状態を正として判定するよう変更。
- 予備Excelダウンロードは通常導線から外し、「送信できない時だけ予備Excelを開く」の折りたたみに移動。`supabase db push` と `npm run build` 済み。

## 2026-07-03 労務士提出Gmail送信設定
- Gmail API送信用のOAuthリフレッシュトークンを取得し、本番Vercelの `GOOGLE_GMAIL_REFRESH_TOKEN` に設定。勤怠提出メールをGmail APIで送信できる状態にした。
- 未設定時のエラー文言は内部の環境変数名を出さず、利用者向けに「メール送信設定未完了」と表示するよう整理。
- 本番管理画面から2026-06分の労務士提出を実行し、`ts@ai.aizu-tv.com` の送信済みで `2026-06 勤怠データ送付（フロア・製造・道の駅）` と3添付ファイルの送信を確認。

## 2026-07-04 FAX受信添付の堅牢化
- 本番のFAX受信掲示板を確認し、直近投稿は画像添付・画像ロードとも正常であることを確認。ただし再発防止としてDocScanner連携の受信APIを強化。
- `dataBase64` / `data_base64` / `base64` / `fileName` / `mimeType` / `previewUrl` など表記揺れを受けられるようにし、URLだけで届いた画像は可能な場合に取得してDriveへ再保存する。相対URLなど壊れた添付はそのまま保存しない。
- `npm run build` 済み。TSG本番デプロイ対象。

## 2026-07-07 掲示板・グループChat画像マーキング
- 掲示板/コメント/グループChatの画像添付に、投稿後から赤枠・赤丸を描いて画像を差し替える共通エディタを追加。
- Google Drive画像をキャンバス保存できるよう、ログイン済みユーザー限定の `/api/image-proxy` を追加。保存は既存の `/api/upload` と添付配列更新APIで処理。
- `npm run build` は成功。`npx tsc --noEmit` は既存の別ファイル型エラーのみ残存。

## 2026-07-07 シフト希望コメント欄
- スタッフ側 `/shifts` の出られない日カレンダー下部に「その他の要望コメント」欄を追加。
- 提出単位の `gw_shift_request_submissions.request_comment` に保存し、管理側の希望一覧でも提出者ごとのコメントを確認できるようにした。
- `202607070001_shift_request_submission_comments` を本番Supabaseへ適用済み。`npm run build` 成功。

## 2026-07-07 タスク画面の表示順と件数整理
- `/tasks` はタスク依頼を上部、メンションを下部に変更。タスク依頼は未完了・完了を同じ一覧でステータス表示する構成に整理。
- タスク依頼・メンションとも初期表示は最新10件にし、残りは「過去を見る」ボタンで展開できるようにした。
- `status=all` のタスク取得は作成日時の新しい順に変更。`npm run build` 成功。
## 2026-07-07 シフト画面ライトテーマ表示補正
- シフト関連画面のライトテーマで、ダーク用の固定背景色と文字色が残って同化する箇所を補正。管理側のシフト作成、希望回収、提出状況、シフト表、スタッフ側の休み希望カレンダー、スマホ用編集カードを対象に `html[data-theme="light"]` の上書きを追加。
- 選択済み、提出済み、未提出、休み希望、割当不可、警告バッジなどの状態色もライト背景で読める配色へ整理。`npm run build` 成功。
## 2026-07-08 労務士提出Excelの勤務時間列除外
- 勤怠の労務士提出用Excelから「勤務時間」列を削除。提出データは「日付 / 曜日 / 出勤 / 退勤 / 30分休憩 / 労務士への連絡備考」の構成に変更した。
- 自社側の打刻データ・勤怠画面・給与計算ロジックには触れず、労務士へ送る添付Excelだけ勤務時間を記載しない運用に合わせた。`npm run build` 成功。
## 2026-07-08 本人用打刻履歴確認
- `/attendance` を打刻操作なしの閲覧専用ページに変更し、各ユーザーが自分の当月打刻履歴だけ確認できるようにした。月切替、無効化済み打刻の表示切替、端末名、管理修正、30分休憩、自家用車情報、備考を表示。
- 本人専用API `/api/attendance/punches` を追加。ログイン中ユーザーの `user.id` で `gw_attendance_punches` を固定検索し、リクエスト側から他ユーザーIDを指定できない構成にした。
- 下メニューに「勤怠」を追加。専用タイムレコーダーの打刻APIは引き続き端末専用で、スマホ側に打刻ボタンは出さない。`npm run build` 成功。
## 2026-07-09 TSG Supabase確認手順メモ
- TSGの本番Supabase確認では `.env.local` ではなく `.env.production` の `NEXT_PUBLIC_SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` を読む。`.env.local` にキーが無いことで調査を止めない。
- DB確認時はキー値を表示せず、件数・月別内訳・対象テーブル名だけを出す。過去に同じ `.env.local` 確認で無駄が発生したため、以後の労務・勤怠・給与データ調査はこの手順を優先する。
## 2026-07-09 労務士ZIP取込と給与差分確認
- 管理 > 労務データに労務士ZIP登録欄を追加。給与月・勤怠月・給与/賞与/調整を指定し、ZIPをそのまま登録できるようにした。ZIP本体はDrive保管を試み、DBには既存の `gw_labor_import_batches` / `gw_labor_source_documents` を使ってZIP本体とZIP内の `.pdf` / `.xls` / `.xlsx` / `.csv` / `.txt` を本文解析待ちとして記録する。
- 支給控除一覧表、事業所負担保険料一覧表、賃金台帳は、今後Excel/CSVが届く場合に優先解析し、PDFは原票・照合用として残す前提を画面文言にも反映。
- `GET /api/admin/payroll/diff` を追加。労務士取込済み給与結果とTSG側の給与計算プロファイル＋打刻試算を給与期間ごとに比較し、差異あり・自社設定なし・労務士結果なしを管理画面で確認できるようにした。`npm run build` 成功。
## 2026-07-09 労務士ZIP取込UI整理
- 管理 > 労務データのZIP取込欄が横詰めで崩れていたため、給与月/勤怠月/種別の設定行と、ZIP選択/登録の操作行に分離。ネイティブのファイル入力を隠し、選択ファイル名とサイズがカード内で収まる専用ボタン表示に変更した。
- スマホ幅では各入力と登録ボタンが縦積みになるようCSSを調整。`npm run build` 成功。
## 2026-07-09 給与計算差分画面の表現修正
- 現在のTSG給与計算ロジックは労務士ロジック復元用の仮試算であり、完成済みの自社給与計算として扱わない。管理 > 労務データの差分欄から「自社支給」「自社手取」「一致」など確定計算に見える表現を外し、「仮試算支給」「仮試算手取」「要確認」「仮一致」へ変更した。
- 休暇、正社員/役員の固定給、通勤費、社会保険、控除、労務士独自の丸めは未完成であることを画面上に注意書きとして表示。`npm run build` 成功。
## 2026-07-09 給与差分の0円比較停止と労務士結果突合補正
- 労務士結果との差分で、打刻が無い時給/残業系スタッフを0円仮試算として比較するのを停止。打刻なしの場合は「打刻なし」と表示し、計算不可・ロジック修正対象として扱う。
- 労務士結果の突合を `employee_id` 直接一致だけにせず、社員NO、正規化名、括弧除去名で強い一致を自動突合補正するよう変更。旧姓の可能性は下の名前一致を「旧姓候補」として表示し、自動確定はしない。
- 差分画面のサマリーに突合補正、候補あり、打刻なしを追加。社員名欄に「突合: 労務士側名 / 社員NO / 突合理由」または候補一覧を出すようにした。`npm run build` 成功。

## 2026-07-09 給与差分の過去勤怠入力補正
- 給与差分APIで端末打刻がない過去月を0日/0:00扱いにせず、給与計算プロファイルの `source_snapshot` に入っている労務士取込済みの出勤日数・就労時間・普通残業・日曜残業を仮試算入力に使うよう変更。
- 給与計算プロファイルの適用判定を勤怠月ではなく給与月基準に修正し、過去労務データの月ずれで誤ったプロファイルを拾わないようにした。
- 差分画面の勤務欄に、端末打刻ではなく労務士取込勤怠で仮試算している場合の表示を追加。これにより、差異なし/0円ではなく、自社ロジックと労務士結果の実差異を確認できるようにした。`npm run build` 成功。

## 2026-07-09 LINE登録URLコピー導線
- ホーム右上ユーザーメニュー内のLINEログインQRを `/login` へ統一し、新規登録・再ログイン用の案内URLとして使えるよう調整。
- QR下にURL表示と「URLコピー」ボタンを追加。コピー成功時は一時的に「コピー済み」と表示する。`npm run build` 成功。

## 2026-07-09 給与差分画面の仮表現と横スクロール解消
- 管理 > 労務データの「労務士結果と仮試算の差分」を「労務士結果と自社計算の差分」へ変更。状態の「仮一致」は「一致」、設定不足は「計算設定なし」に変更した。
- 差分一覧を横スクロールする表から縦カード型に変更。支給・手取の労務士結果/自社計算/差額をカード内に収め、スマホ幅では1列表示にして横スクロールを出さないよう調整。`npm run build` 成功。

## 2026-07-09 給与差分のロジック追跡強化
- 給与差分APIで労務士側の給与明細項目を読み込み、基本給・残業/休日/深夜・手当/その他課税・非課税・控除ごとに自社計算との差を返すようにした。画面には「差異理由」と内訳比較、労務士明細の折りたたみ表示を追加。
- 管理 > 労務データ上部の取込バッチ一覧と原文ファイル一覧を表からカード表示へ変更し、この画面内に残っていた横スクロールを解消。`npm run build` 成功。

## 2026-07-11 シフト希望の後修正と並び順
- スタッフの休み希望は「希望回収中」だけでなく「下書き作成済」「調整中」でも後から修正できるように変更。ホーム上部のシフト希望カードも修正可能な期間は表示する。
- 管理 > シフト作成のシフト表セルに「休」トグルを追加し、管理者が所属内スタッフ全員の休み希望を後からON/OFFできるようにした。休み希望ON時は同日の割当を削除する。
- シフトスタッフの並びを正社員系（入社日順）→パート系（入社日順）→その他に変更。`npm run build` 成功。

## 2026-07-11 AIシフト制約ロジック確認
- 現行AI下書きは、日別必要人数、休み希望日の除外、勤務形態別の勤務パターン、フロアの早番・遅番バランス、個人別の勤務パターン傾向を使用している。
- 個人別の最大出勤日数、最低休日数、最低/目標出勤日数、連勤上限、期間/週の勤務時間上限は未実装。最大出勤日数と最低休日数は期間日数に対して重複するため、両方入力された場合は厳しい方を上限として扱う設計が必要。
- 実運用の自動作成には、正社員系の所定日数、パートの週29.5時間上限、フルタイムパートの目標日数/時間、日別・時間帯別必要人数をハード制約として追加し、充足不能時は警告を返す。

## 2026-07-11 AIシフト勤務条件実装
- スタッフのシフト希望画面に、最大出勤可能日数、希望出勤日数、最低希望休日数、最大連続勤務日数を追加。従来のカレンダー休み希望と自由形式の備考・その他要望も維持した。
- 希望条件を `gw_shift_request_submissions` に保存するDB列を追加し、TSGリンク先Supabaseへ適用・列存在確認まで実施。過去提出データは勤務形態別の既定値で補完する。
- AI下書きを制約付き割当に変更。休み希望、期間出勤上限、最低休日、最大連勤、5日/6日正社員の週日数、パートの週29.5時間、前期間から続く連勤、個人別勤務時間傾向、フロア早番/遅番バランスを反映する。
- 希望出勤日数への不足が大きいスタッフを優先して割り当て、必要人数を満たせない場合は休み希望・出勤上限・連勤上限・週日数・週29.5時間・勤務時間未設定の除外理由を警告する。管理画面には各人の提出条件と自由備考を表示する。
- AI下書き再実行時は以前のAI割当だけを削除して再生成し、管理者の手動割当は保持する。`npm run build` 成功。

## 2026-07-11 シフト備考のECセール選択
- 管理 > シフト作成の日別備考に、楽天3種、Amazon3種、Yahoo4種のECセールを複数選択できる折りたたみチェックボックスを追加。楽天は赤、Amazonは緑、Yahooはオレンジで表示する。
- 選択値は `gw_shift_requirements.ec_sale_tags` に配列保存し、任意の自由テキスト備考は従来の `notes` に保存する。TSGリンク先Supabaseへ列を適用し、配列型であることを確認した。
- PCは備考セル内のセレクター、スマホは日別カード内展開にして横はみ出しを防止。選択済みは色付きタグで表示する。
- Excel出力では選択したセール名と任意テキストを結合して一つの備考欄へ出力する。`npm run build` 成功。

## 2026-07-11 希望回収テストモード
- 管理 > シフト作成に `希望回収テスト` ボタンを追加。実スタッフへ配信せず、所属する全スタッフ分の最大出勤日数、希望出勤日数、最低休日数、最大連勤、休み希望日、自由備考を整合する範囲でランダム生成する。
- シフト期間に `is_test_mode`、希望・提出に `is_test` を追加。スタッフ向けAPIはテスト期間を明示的に除外するため、AI下書き後に状態が変わってもホームと希望入力画面には表示しない。
- テスト中は画面上部と作成済みシフト一覧にテスト表示を出し、シフト確定を禁止。実希望や実回収対象が存在する期間ではテスト生成を拒否して上書きを防ぐ。
- 通常の希望回収を開始した場合は、テスト希望・テスト提出・テスト由来AI割当のみを自動削除し、管理者の手動割当は保持する。希望全削除ではテストAI割当も消して期間を下書きへ戻す。
- TSGリンク先Supabaseへ3列を適用・存在確認済み。`npm run build` 成功。

## 2026-07-11 シフト印刷プレビュー
- ECセールのチェック画面に `キャンセル` と `備考に追加` を追加。チェック操作だけでは保存せず、追加ボタンを押した時に日別備考へ反映する方式へ変更した。
- 添付 `シフト.pdf` を横向き印刷レイアウトの基準として確認。管理 > シフト作成に `印刷プレビュー` を追加し、専用画面 `/admin/shifts/print` で印刷前の完成形を表示する。
- 印刷表はスタッフ列、所属人数、所属備考、備考2、日付、曜日の順で構成。勤務ありは薄緑、休み希望は黒、勤務なしはグレー、楽天/Amazon/Yahooのセール文字は赤/緑/オレンジで表示する。
- 前半15日と後半最大16日は各1ページに収め、スタッフが8名以上の場合は7名単位で自動改ページする。A4横、背景色を含む印刷CSS、色凡例、画面上の戻る/印刷ボタンを追加。印刷時は操作UIと下メニューを非表示にする。
- 印刷プレビューでは下メニューに加えて引っぱって更新も無効化し、印刷表と専用ツールバー以外のUIが混ざらないようにした。
- `npm run build` 成功。

## 2026-07-11 シフト作成上部の導線整理
- 重複していた `開くシフト` の所属/期間プルダウンと更新ボタンを削除。作成済みシフトの行をクリックして開く操作へ一本化した。
- 所属選択は新規シフト作成欄へ移動し、フロア・製造・道の駅の作成/一覧切替機能を維持した。
- 作成済みシフトは最新5件だけを初期表示し、6件目以降は `過去のシフトを表示` ボタンで展開、`最新5件に戻す` で折りたためるようにした。`npm run build` 成功。

## 2026-07-11 シフト印刷の日付・休日・人数補正
- 印刷表の日付・曜日列を右端から左端へ移動。日曜日に加え `gw_holidays` に登録された祝日は日付と曜日を赤文字で表示し、祝日名をセル情報に付与する。
- 2026年祝日テーブルが空だったため、内閣府公表の国民の祝日・休日18日を `gw_holidays` へ登録し、件数と期間を確認した。
- フロア人数は、その日に藤田香織が勤務している場合だけ全勤務人数から1名差し引いて表示する。必要人数不足の色判定も差引後人数を使用する。
- スタッフ枠の色を、希望休日=グレー、希望がなく会社側で割り振った休日=黒、勤務あり=薄緑へ変更。印刷下部の凡例も同じ文言と配色へ変更した。`npm run build` 成功。

## 2026-07-11 ECセール項目マスタ・シフト表セル色
- コード固定だったECセール候補を `gw_shift_ec_sales` マスタへ移行。管理 > シフト作成から名称・赤/緑/オレンジ色の変更、新規追加、削除、再表示ができる。削除は過去シフトの表示を壊さない非表示方式とした。
- シフト表にセル色ツールバーを追加し、日付、必要人数、各スタッフ、Timee、備考のマスを期間・日付・マス単位で `gw_shift_cell_styles` に保存する。色なしで個別解除でき、印刷プレビューにも反映する。
- 色は基準PDFに合わせ、勤務時間希望、物販対応、レクチャー、猪苗代納品日、伝票出し、シフト調整してくれた方、会社付与休日、火・土の物販対応設定日の8種を用意。印刷下部にも同じ名称と配色の凡例を表示する。
- TSGリンク先Supabaseへマスタ・セル色テーブルを適用し、初期10項目とテーブル存在を確認。`npm run build` 成功。`npx tsc --noEmit` は今回変更外の既存型エラーのみ残存。

## 2026-07-12 誓約管理・本人同意・PDF保管
- 管理に `誓約` タブを追加。タイトル、本文、チェック項目、同意ボタン文言、会社名を個別編集・保存でき、全員、フロア、製造、道の駅、個別を選んで配信できる。テスト送信はAPI側で佐藤正彦1名に固定する。
- 初期テンプレートとして「会社機密情報に関する誓約」、確認4項目、重要事項、`上記内容を確認し、誓約する`、`株式会社テクニカルスタッフ` を登録。配信時に内容をスナップショット化し、後日のテンプレート編集で配信済み内容が変化しないようにした。
- 未提出誓約はホーム最上部に大きく表示し、提出までアカウント単位で残る。ホーム表示中も1分間隔・再フォーカス時に未提出を再確認する。本人画面は全チェック完了時のみ送信可能で、送信後は会社名、誓約者名、誓約日を記載した日本語PDFを生成する。
- 署名済みPDFはGoogle Driveへ非公開保存し、本人認証を通す `/api/pledges/pdf` からのみ閲覧可能。本人の自己DMへPDF添付投稿を作成し、DMから確認できる。
- テンプレート、配信、個人別提出をDB化。提出済み記録はアカウント削除後も氏名・所属スナップショットとPDF情報を保持する。処理中断時は10分後に再提出可能へ戻す。
- Noto Sans JPをPDFへ埋め込み、会社名・日本語・チェック記号の実生成を確認。Supabaseへテーブルと初期テンプレートを適用し、テンプレート1件、確認項目4件、佐藤正彦テスト対象1件を確認。`npm run build` 成功。
- 本人画面・署名PDFの表示順を、冒頭本文→チェック項目→【重要】以下へ変更。【重要】以下とチェック文章の太字を解除し、スマホで潰れにくい通常ウェイトへ統一した。
- 可変日本語フォントとpdf-libの組合せで字幅・欠字が壊れる問題を確認し、静的Noto Sans JP + PDFKitの日本語サブセット生成へ変更。代表文をPDF→PNGへレンダリングし、連続した日本語、氏名、日付が正常表示されることを目視確認した。
- DM添付のPDF直リンクをTSG内PDFビューアへ変更し、上部にDMへ戻るボタンを常設。既存の提出済み添付リンクもDB更新し、PDF本体は保存済みスナップショットから最新生成処理で再描画するため再提出不要とした。
- 本番送信時にPDFKitがビルド環境のHelvetica絶対パスを参照して失敗する問題を修正。同梱Noto Sans JPを生成開始時の初期フォントに指定し、佐藤正彦のテスト誓約で提出済み更新、非公開Drive保存、本人DM添付、認証付きPDF取得まで本番確認した。

## 2026-07-14 佐藤正彦のシフト希望回収除外
- 佐藤正彦をシフト希望回収の恒常的な対象外に設定。管理画面の対象一覧・全員選択、通常配信API、希望回収テスト生成、本人の希望提出APIから除外し、シフト表のスタッフ表示と勤務割当は維持する。
- 道の駅 2026-07-01〜07-15に残っていた本人の希望5件と提出記録1件を削除。既存シフト割当49件は変更せず、本番で本人の回収期間0件、管理画面の対象外判定、直接提出403を確認した。`npm run build` 成功、本番デプロイ済み。

## 2026-07-15 LINEユーザーと人事管理の自動連携
- 承認済みTSGユーザーを人事・勤怠・給与の `gw_payroll_employees` へ自動連携するDBトリガーを追加。LINE登録直後の承認待ちは作成せず、管理者が承認した時点で人事マスタを作成する。
- 本名・所属変更は人事側へ追従し、勤務先もフロア=会津ブランド館、製造=本社、道の駅=道の駅へ同期する。社員NO、入社日、勤務形態、給与履歴など人事固有情報は上書きしない。
- 同名の未連携かつ在籍中の人事データが1件だけなら既存行へ連携し、複数候補時は誤連携を避けて新規作成する。新田奈美を在籍・道の駅として連携し、人事管理API表示と重複なし1件を本番確認した。

## 2026-07-15 履歴書AI OCRと社員NO自動連番
- 管理 > 人事管理に履歴書PDFのアップロード、差し替え、別タブ閲覧、AI再読取、削除を追加。PDFはSupabaseの非公開 `hr-documents` バケットへ保存し、人事権限API経由でのみ取得する。
- Gemini AI OCRで氏名カナ、生年月日、性別、住所、電話、メール、学歴、職歴、資格、志望動機、補足を抽出し、既存値を上書きせず空欄だけを補完する。氏名不一致は自動変更せず警告する。DocScannerで稼働中の有効なGemini認証へTSG本番設定を更新した。
- 社員NOを最大値からの単調増加シーケンスに変更。退職・削除後も番号を再利用しない。新田奈美=145、既存未採番者=146〜148を採番し、在籍TSG連携者の未採番0件、重複0件を確認した。
- 合成履歴書で本番の非公開保存、OCR、人事11項目反映、PDF取得、削除まで通し試験済み。試験社員・DB文書・ストレージ実体はすべて削除済み。`pnpm build` 成功。
- DocScanner用 `POST /api/integrations/doc-scanner/hr-resume` を追加。既存のPDF保存・OCR・人事反映を共通サービス化し、secret認証、書類IDの冪等制御、一意氏名での既存人事紐付け、0件時の未連携人事作成、複数候補時409返却に対応した。本番で401、一意一致、再送重複、新規作成、同名409を確認し、試験データは全削除済み。

## 2026-07-15 履歴書画面復帰時の自動更新
- DocScannerから履歴書を取り込んだ後も、既に開いていた人事管理画面が古い「未登録」状態を保持する問題を修正。画面・タブへの復帰時に人事一覧をバックグラウンド再取得し、開いている社員欄や検索状態を維持したまま最新の履歴書とOCR結果を表示する。
- 新田奈美（社員NO 145）で履歴書PDFとOCR結果が本番に保持されていること、手動更新で表示が復帰することを確認。`pnpm exec eslint app/admin/page.tsx` はエラー0件、`pnpm build` 成功。

## 2026-07-15 採用連絡メール内容の保存
- 管理 > 人事管理の社員詳細に「採用連絡メール内容」欄を追加。採用連絡メールの件名・本文を長文のまま貼り付け、既存の「人事情報保存」で社員別 `hr_profile` に保存する。
- 履歴書AI OCRでは変更しない手入力専用項目とし、退職者削除時は他の個人情報と同様に内容を消去する。対象Lintはエラー0件、`pnpm build` 成功。
## 2026-07-18 シフト表の日付セル色変更

- シフト入力表の左端の日付セルを、他のセルと同じ色パレットで変更できるようにした。
- 固定列の背景指定に選択色が負けないよう、ダーク・通常モード共通で日付セルの色を優先表示する。

## 2026-07-18 シフト作成の所属タブ化

- シフト作成画面の所属プルダウンを廃止し、フロア・製造・道の駅を上部タブで切り替える構成へ変更。作成済み期間、対象スタッフ、勤務時間候補を所属ごとに読み直す。
- シフト期間が未作成の所属でも、最初のシフト表から登録した勤務時間候補と対象スタッフを表示するようAPIを調整。フロアの早番・遅番配分、製造の多人数2画面、道の駅の個人別時間傾向は従来どおりAI下書きへ反映する。
- `pnpm exec eslint components/shift-admin-tab.tsx app/api/admin/shifts/route.ts` はエラー0件、`pnpm build` 成功。

## 2026-07-18 TSG君のシフト対象除外

- AIアカウントのTSG君を通常スタッフのシフト対象から除外。所属変更後も管理画面、AI下書き、印刷、Excel出力へ混入しない共通判定とした。
- 過去に残ったTSG君の割当も必要人数・割当件数へ加算しないよう、シフト読込時に有効な対象スタッフで絞り込む。

## 2026-07-18 フロア必要人数・希望提出期限

- フロアのAI下書き前にGoogle連携済みTSGカレンダーを同期し、楽天・Amazon・Yahooのうち同日に2モール以上のセールが重なる日は必要4名、その他は3名へ更新する。同一モール内で複数セールが重なっても1モールとして数える。同期失敗時は保存済みカレンダーを使用して警告を表示する。
- 藤田香織は管理者の予備要員として扱い、他スタッフだけで必要人数を満たせる日は `基本勤務`、不足する日だけ `フロア勤務` をAI下書きで割り当てる。休み希望・出勤上限・連勤条件は通常どおり適用する。
- スタッフの希望回収画面とホーム上部に提出期限を常時表示。締切3日前から黄、当日・期限超過は赤で目立たせる。`pnpm build` 成功。

## 2026-07-19 スマホセッション継続修正

- 2026-06-18に一時発行した発行時刻入りCookieが30日後に固定失効し、旧形式へ戻した後も既存端末だけ7月18〜19日に再ログイン判定される問題を修正。旧Cookieは有効なユーザーIDを維持したまま現行形式へ自動移行する。
- ログイン済みアクセスごとにCookie期限を30日へ延長する方式へ戻し、利用中の端末が一斉失効しないようにした。古いVercel別名で開いた場合も正式な `v0-line-blush.vercel.app` へ統一する。
- `pnpm exec eslint` はエラー0件、`pnpm build` 成功。

## 2026-07-20 製造スタッフの基本勤務時間

- 人事管理の基本情報に、基本勤務の開始・終了時刻と休憩分数を追加。開始・終了の両方が設定されたスタッフだけをシフト生成の個人別優先時間として扱う。
- 手書き資料の製造10名について、製造所属と氏名を照合して基本勤務時間を初期登録。フロア等の同姓スタッフには反映しない。
- 製造シフトのAI下書きは個人別基本勤務を勤務パターンより優先し、各日セルでは開始・終了・休憩を直接手入力して一時保存できる。確定保存後の編集禁止は従来どおり維持。対象Lintはエラー0件、`npm run build` 成功。

## 2026-07-20 シフト希望条件の任意入力

- スタッフの希望回収で、最大出勤可能日数・希望出勤日数・最低希望休日数・最大連続勤務日数をすべて任意入力へ変更。休み希望も数字条件もない状態で提出できる。
- 空欄は希望値として補完せずDBへ `null` のまま保存し、AI下書き時だけ就業形態別の標準値を適用する。選択した出られない日は標準値にかかわらず勤務候補から除外する。
- 画面に現在の自動値（出勤目安、上限、最低休日、最大連勤）を表示。パートの週29.5時間上限判定は従来どおり維持。対象Lintはエラー0件、`npm run build` 成功。

## 2026-07-20 シフトのリセット・会社休・自由時刻入力

- 「変更をキャンセル」とは別に「シフトをリセット」を追加。勤務割当と会社指定休日を削除してAI下書き前へ戻し、スタッフの希望休・回収状況・日別条件は保持する。
- スタッフ希望休と会社指定休日を別データとして扱い、PC表とスマホ日別画面の両方で「会社休」を明示。AI下書きで勤務を割り当てなかった日も会社休として保存する。
- フロア・道の駅の各スタッフセルに出勤・退勤の直接入力ボタンを追加。登録済みパターンを使わない任意時刻も保存できる。製造の既存直接入力は維持する。
- 対象Lintはエラー0件、`npm run build` 成功。
- 作成済みシフト一覧の各行にも「シフトのみリセット」を追加。対象期間を開かずに、提出済み希望・回収状況・自由記入・日別条件を残したまま勤務割当と会社休だけを初期化できる。

## 2026-07-20 道の駅Timee入力

- 道の駅シフトのTimee欄を人数入力から、自由テキスト・開始時刻・終了時刻の入力へ変更。フロア・製造の人数入力は維持する。
- 道の駅ではECセールの選択・管理を表示せず、印刷プレビューとExcel出力にもECセール情報を含めない。
- Timeeの内容と時間帯は印刷プレビュー・Excelへ反映。対象Lintはエラー0件、`npm run build` 成功。
- 勤務パターン選択済み、または出退勤時刻を直接入力済みのスタッフセルは淡いクリーム色で表示し、未設定セルと即座に見分けられるようにした。PC表・スマホ日別画面の両方へ反映する。

## 2026-07-20 シフト一時保存の検証・修正

- 複数セルを続けて変更した際、古い画面状態で後続変更が上書きされる競合を修正。日別条件、勤務割当、直接時刻、会社休、休み設定を最新状態へ積み上げる。
- 保存APIは割当・セル色を全件削除してから再挿入する方式を廃止。先に安全にupsertし、成功後に不要行だけ削除する。API保存件数と画面件数、保存後の再読込成功も照合する。
- UIは調整中シフトを「一時保存済み」と明示。本番の道の駅8/20で勤務割当、Timee自由文、開始・終了時刻を保存し、再読込・印刷反映を確認後、検証値を全て元へ戻した。対象Lintはエラー0件、`npm run build` 成功。

## 2026-07-20 一時保存後の未設定・会社休判定

- 一時保存で期間ステータスが `editing` へ変わった際、割当レコードのない未設定セルまで会社休として表示していた判定を修正。未設定は保存前後を問わず未設定のまま表示する。
- 会社休は会社休ボタンで作成した明示レコードだけを対象とし、PC表・スマホ日別画面で同じ判定を使用する。本番の一時保存済みフロアシフトで、未設定と既存会社休が正しく区別されることを確認した。

## 2026-07-22 誓約書の未提出警告・提出状況

- 誓約書送信から7日経過で提出警告、14日経過で最終警告へ自動切替する。最終警告には「提出がない場合、次回シフトから勤務できません」と明記する。テスト送信は警告集計から除外する。
- 未提出者のホーム上部バナーと誓約書本文画面へ段階警告を表示し、複数の未提出がある場合は最も緊急度の高い誓約書を優先表示する。
- 管理の誓約画面に個人別の提出状況を追加。未提出・7日警告・14日最終警告・提出済みの件数、氏名、所属、送信後日数、提出日時を確認でき、未提出／全件／提出済みで絞り込める。
- 本番では実配信24名中23名提出、未提出1名（送信から6日）を正しく表示。PC・390pxスマホ表示で横崩れがないことを確認し、対象Lintはエラー0件、`npm run build` とVercel本番ビルド成功。

## 2026-07-22 紙の誓約書・DocScanner保存

- 管理 > 誓約に `紙で印刷` を追加。印刷時点のタイトル・本文・チェック項目・会社名を紙原本用配信として固定保存し、A4原本へ一意の `誓約書No. TSG-PLG-YYYYMMDD-XXXXXXXX` を印字する。本人の手書き欄は氏名と誓約日のみ。
- DocScanner連携用 `POST /api/integrations/doc-scanner/pledge` を追加。secret認証、誓約書No.による原本スナップショット特定、承認済みTSGユーザーとの氏名一致、原本PDFのGoogle Drive非公開保存、提出済み記録作成、原本ハッシュによる冪等再送に対応した。
- 管理の提出状況ではDocScanner取込を `紙提出` と表示し、管理者だけがスキャン原本を別タブで閲覧できる。通常のオンライン提出PDFと紙原本を同じ提出状況で管理する。
- 本番A4画面で誓約書No.、本文、空チェック欄、手書き氏名・日付欄を確認。固定下メニューを印刷画面から除外し、横はみ出し0を確認した。対象Lintはエラー0件、`npm run build` とVercel本番ビルド成功。
## 2026-07-22 誓約書テンプレート管理

- 誓約管理に作成済みテンプレートの切替、新規作成、複製、保管・再開を追加。
- 別内容の誓約書を独立して保存・配信・紙印刷でき、保管後も過去の配信・提出履歴は保持する。

## 2026-07-23 掲示板コメントの常時表示

- コメントがある投稿は、投稿一覧を開いた時点で最新5件を本文下へ表示する。
- 6件以上は残件数付きの「さらに表示」ボタンから全件を読み込み、コメント入力欄は「コメントする」を押した時だけ表示する。

## 2026-07-25 シフト希望の再編集期限

- スタッフは提出済みのシフト希望を締切当日まで再編集でき、翌日以降は編集不可とする。
- JST基準の共通締切判定を画面とAPIへ適用し、締切後の直接API更新も拒否する。

## 2026-07-25 道の駅シフト列幅

- 道の駅のPC版シフト表だけTimee列を220pxへ拡張し、内容・開始時刻・終了時刻を入力しやすくした。
- 備考列を108pxへ縮小し、その他の列は残り幅へ均等配分する。スマホのカード表示は従来どおり。

## 2026-07-25 印刷プレビューの色凡例

- 勤務時間希望・物販対応などの色凡例はフロア専用とし、製造・道の駅の印刷プレビューでは表示しない。

## 2026-07-25 シフトのスタッフ表示順

- 道の駅の初期表示順を、佐藤正彦、佐藤ちさと、生井（内海）、武藤、角田、新田の順にした。
- シフト対象スタッフとシフト表の名前見出しからドラッグで順序を変更でき、所属単位の表示順として次回以降にも保存する。

## 2026-07-25 シフト表の分割スクロール

- PC版シフト表はスタッフ名の見出し行を上部に固定し、その下の日別シフト行だけを縦スクロールする。
- 横方向は見出しと本文を同じスクロール領域に置き、列位置がずれないようにする。スマホのカード表示は従来どおり。
- スクロール末尾に固定下メニュー分の余白を確保し、最終日行がメニューの裏へ隠れないようにする。

## 2026-07-26 有給・欠勤管理

- 入社日と週所定労働日数から法定付与日・付与日数を算出し、全休・半休、残数、次回付与、出勤率、欠勤回数を管理する。有給付与条件は出勤率80%以上。
- 初回導入時は過去の全労日データが揃わないため出勤率をみなし、最新の未失効付与分だけを初期登録する。過去繰越・既使用分は管理画面の残高調整で補正する。
- シフト希望回収に有給全休・半休を追加。確定シフトと有効な出退勤打刻を照合し、未打刻日は本人ホームで有給・欠勤・打刻忘れ・シフト変更を確認する。
- スタッフ本人がシフト希望提出後の有給を申請し、管理者は申請承認、欠勤確認、残高調整を行える。佐藤正彦・佐藤ちさとのみ、対象者本人と同じ有給画面を読み取り専用で代理確認でき、操作履歴を保存する。
- パート系の有給賃金時間は直近3か月の平均勤務時間を優先する。初期値は `シフト表2026 のコピー.xlsx` から登録したが、同ファイルに賃金・時給情報はないため金額は人事・給与側の有効単価から計算する。
- 佐藤葵はフロア・製造の同日重複行が多いため、Excel由来の平均時間を自動登録していない。人事管理または実績蓄積後に確認する。
- DBマイグレーションは `202607260001_paid_leave_management.sql` と `202607260002_paid_leave_shift_workbook_seed.sql`。有給消化・申請確定・却下・再開・シフト一括反映はDB関数内のトランザクションで処理する。
- 2026-07-26、有給対象の在籍者全員を再監査。短期間のTSGシフトを年間勤務日数と誤認してパートの付与見込が0日になる問題を修正し、過去シフト表から週所定日数を推定するよう変更。旧残高4名を除く15名へ2026-08-01の制度開始時みなし付与を登録し、3名は入社6か月後の初回付与待ちとした。詳細は `docs/paid-leave-transition-review.md`。
- 全スタッフ向け運用案内は2ページへ簡略化し、ホームの希望回収、有給全休・半休と日付の選択、残日数確認のスマホ画面を番号付きで掲載する。配布用のDOCX・PDFと元Markdown、再生成スクリプトを `docs` / `scripts` に保持する。

## 2026-07-26 有給画面の出勤率・参考時間修正

- 確定シフト運用開始前は全労働日を確定できないため、2026-08-01〜2026-10-31は出勤率を「計測中」とする。2026-11-01から、確定シフトと打刻を使った直近3か月の参考出勤率を表示する。
- 画面の3か月値は日常確認用。法定付与の80%判定は、初回は入社後6か月、その後は各付与日前1年間の全労働日で別途計算し、参考表示とは分離する。
- 直近3か月の平均勤務時間は給与計算と同じ15分丸め・休憩控除後の実勤務時間で集計する。実打刻がある場合は旧シフト表スナップショットで上書きしない。
- 5日正社員・6日正社員・役員は1日参考賃金を時給換算せず「月給に含む」と表示する。佐藤葵（フロア、社員コード146）は5日正社員として固定し、2026-08-01に11日付与、次回2027-06-17に12日見込みとする。

## 2026-07-26 藤田香織の入社日訂正

- 藤田香織の入社日を2012-04-02から2012-02-12へ訂正する。現在の有給残16日は維持する。
- 個人基準日を10月2日から8月12日へ変更し、2026-08-12の法定付与見込みを20日とする。移行調整休暇は2026-08-01の1日のみ残し、不要になった2026-09-01・2026-10-01の将来付与は監査履歴を残して無効化する。
- 4名分の個別お知らせを同じ構成の配布用文面へ更新した。藤田香織向けには入社日訂正、8月1日の移行調整1日、8月12日の通常付与20日、旧案の9月・10月調整付与取消を明記する。

## 2026-07-26 有給の本人申請・管理者承認

- 管理画面の「突発有給を登録」を廃止し、各スタッフの有給画面へ「有給申請」を追加。確定シフト上の勤務日を全休・半休で本人が申請する。
- 有給申請の常設入口はホームへ置かず、一般スタッフは下メニューの「管理」内にある「有給申請」から開く。一般スタッフには有給申請以外の管理機能を表示しない。
- 同日重複、承認待ちを含む残日数、取得日時点で有効な将来付与分、確定シフトの所定時間をサーバー側で検証する。
- 希望回収以外の本人申請は、承認待ちがある時だけ管理者ホームへ承認・却下ボタン付きで表示し、処理後はホームから消す。管理者へWeb通知し、承認・却下後は本人へもWeb通知する。
- 375pxスマホ幅で申請ボタンの横はみ出しがないこと、管理画面から旧登録欄が消えたことを本番で確認。`npm run build`、Vercel本番デプロイ成功。

## 2026-07-26 有給管理対象外アカウント

- 佐藤正彦・佐藤ちさと・TSG君は有給管理対象外。本人残高、本人申請、未打刻有給確認、管理者の対象者一覧、自動付与の全経路から除外する。
- 対象外アカウントに誤作成された付与ロットと未確定申請は削除せず無効化し、監査ログに理由を保存する。

## 2026-07-27 シフト・有給・勤怠・社内給与の整合性監査

- 確定シフトの有給希望は必ず有給台帳へ同期してから確定し、汎用更新APIから同期を迂回して確定できないようにした。実シフトの確定期間重複も拒否する。
- 有給（全休）と実打刻の重複をAPIとDBトリガーで防止し、既存重複は給与試算で二重計上せず警告する。残数移行用の `opening_balance_adjustment` は残高だけに反映し、勤務実績・給与・出勤率・労務士提出から除外する。
- 打刻は同日の複数出退勤を区間ごとに組み合わせ、区間外の空き時間を勤務時間へ含めず合算する。片側打刻は給与試算から除外して警告し、労務士提出備考にも複数区間または未完了打刻を明記する。
- 社内給与、有給3か月平均、有給の提出用擬似打刻は、適用日時点の15分丸め・休憩ルールをDBから共通取得する。有効期間内の給与プロファイル・時給だけを使用し、月途中入社者も当月試算対象に含める。
- 本番監査は、確定シフト有給の同期漏れ、残数割当不一致、有給スナップショット欠損、全休有給と実打刻重複、確定期間重複、打刻の人事未連携、在籍者の給与プロファイル欠損がすべて0件。監査は `pnpm audit:workforce` で再実行できる。
- 2026-07-22の渡部瞳の20:51出勤は退勤時刻を推測できないため自動修正せず、同日の完結した2勤務区間だけを給与試算へ含める。管理者が正しい退勤時刻を確認後に勤怠修正する。

## 2026-07-29 有給残日数の全員一覧

- 管理 > 有給・欠勤に、対象スタッフ全員の現在の有給残日数をフロア・製造・道の駅別で表示する一覧を追加した。
- 一覧は付与済み・未失効の付与ロット残数を一括集計するため、スタッフ人数分の個人APIを呼ばない。名前を押すと同じ画面の個人詳細へ切り替わり、追加付与・使用済み調整後は一覧も同時に更新される。
- 2026-07-29、有給基準日移行の会社独自調整休暇を月次付与から2026-08-01一括付与へ変更。藤田香織1日、渡部瞳2日、武藤志保8日、石井瑞季9日を8月1日にまとめて付与し、登録済みの9月以降の月次ロットは監査履歴を残して無効化する。
- 管理 > 有給・欠勤管理の全員一覧へ、現在残数に加えて入社日、次回付与日、付与予定日数を表示。登録済みの将来付与があれば最も近い実付与を優先し、なければ有給プロファイルの次回法定付与見込みを表示する。
- 有給残一覧の氏名、社員NO・入社日、次回付与、残日数、所属見出しを一段階拡大し、3所属の一覧性を保ったまま読みやすさを調整。
- 有給残一覧の次回付与日と付与予定日数を省略表示せず、列幅が狭い場合は項目単位で折り返して全て表示する。個人詳細はスタッフ行全体のクリックで開き、重複する矢印や「詳細」表記は置かない。
- ホーム上部のシフト丸ボタンから、確定保存済みシフトを全スタッフが閲覧できる画面を追加。本人所属を初期表示し、フロア・製造・道の駅を切替可能。スマホでは日付別カードでスタッフ、勤務区分、時刻、会社休、必要人数、備考を表示し、本人行を強調する。希望提出は同画面の切替タブに維持。
- 確定シフトのスマホ表示は日別カードから印刷・Excelと同じ日付行×スタッフ列の表へ変更。縦画面は固定した日付列を基準に横スクロールし、横画面では上部ヘッダーと下メニューを隠して全画面表として表示する。希望休、会社休、任意セル色、日曜祝日色も反映し、内部値 `__company_off__` は画面へ出さない。
- シフト管理の所属別勤務候補マスタは、全幅の罫線だけの見出しを廃止。カード枠、青いアクセント、件数、開閉アイコンをまとめ、PC全画面でも編集入口を見分けやすくした。
- 確定保存後も管理者が「備考を修正」から日別備考だけを修正・保存できるようにした。確定状態、勤務割当、休み、有給同期は変更せず、公開シフトと印刷・出力へ備考のみ反映する。
- 有給管理対象外の佐藤正彦・佐藤ちさと・TSG君は従来どおり除外。本番で22名の表示、個人詳細切替、PCと390pxスマホの横はみ出しなしを確認し、Vercelへデプロイ済み。

## 2026-07-30 有給付与説明と法定基準の最終照合

- 年次有給休暇の通常付与は、入社6か月後、その後1年ごと、出勤率80%以上、週30時間以上または週5日以上は通常付与、週30時間未満かつ週4日以下は所定労働日数に応じた比例付与とし、労働基準法第39条の付与基準と照合した。
- 2026年8月1日の付与表示には、制度開始時のみなし付与と、従来の8月更新から入社日基準へ移す際の不利益防止用の会社独自調整が含まれる。通常の法定付与と混同しないよう、スタッフ向けDMでは「今回付与（制度開始時・移行調整を含む）」と明記する。
- DMの次回付与日数は予定値として案内し、実際の付与は対象期間の出勤率80%以上を確認して確定する。有給管理対象外の佐藤正彦・佐藤ちさと・TSG君には送信しない。
- 2026-07-30、山口侑哉は当月退職予定のため送信対象から除外し、有給管理対象21名へTSG君名義の個別DMを送信した。各DMには8月1日の今回付与、付与後予定残、次回法定付与日、次回付与予定日数を本番台帳から差し込んだ。送信21件、失敗0件。再実行時は21件すべて送信済みとしてスキップされ、重複送信がないことを確認した。

## 2026-07-30 ユーザー権限を3階級へ統一

- TSGアカウント権限を「役員・管理者・ユーザー」に統一。佐藤正彦・佐藤ちさとを役員として固定し、役員は管理者機能を継承する。
- ユーザー・グループ・勤怠・有給管理・シフト・誓約は役員と管理者、給与計算・人事管理・労務データ・本人画面代理閲覧は役員のみ利用可能とした。ユーザー本人の有給申請は従来どおり利用できる。
- 管理機能一覧とユーザー管理・設定画面へ、役員（王冠）・管理者（盾）・ユーザー（人物）の共通アイコンを表示。グループ内の「グループ管理者」はアカウント権限と分離して維持する。
- Supabase migration `202607300001_user_access_levels.sql` を本番適用。PC・390pxスマホで権限表示、役員2名の固定、横はみ出しなしを確認し、Vercel本番へデプロイ済み。

## 2026-07-30 全ユーザー向けマニュアル

- 管理画面のマニュアルを役員・管理者限定から全ユーザー閲覧可能へ変更。一般ユーザーには有給申請とマニュアルだけを表示し、既存の管理権限は広げない。
- マニュアル内に「有給取得」「勤怠・打刻」の切替を追加。有給取得は希望回収からの予定申請、管理からの個別申請、管理者承認、残日数・履歴確認、半休時の打刻まで現行運用に合わせて記載した。
- 勤怠マニュアルの権限表を役員・管理者・ユーザーの現行3権限へ更新し、給与・人事・労務は役員限定、マニュアルは全ユーザーと明記した。
- `pnpm lint`、`pnpm build`、Vercel本番ビルドを確認。375pxスマホで横はみ出しなし、権限アイコンとマニュアル切替の表示を本番確認済み。

## 2026-07-30 シフト表セルの代表色

- 通常セルの「その他の色」からOS標準のグラデーション式カラーピッカーを廃止し、赤・オレンジ・黄・緑・水色・青・紫・グレーの8色スウォッチへ変更。
- 日付セルの独立した色設定と、既存の用途別セル色はそのまま維持。色を押すと即時反映してパレットを閉じる。
- `pnpm lint`、`pnpm build`、Vercel本番ビルドを確認。本番シフト画面でスウォッチ8色、ネイティブ色入力0件、コンソールエラー0件を確認。

## 2026-07-30 ブランド館店舗SALE

- フロアのシフト作成にあるECセール一覧へ、赤表示の「ブランド館店舗SALE」を先頭項目として追加。開始・終了時刻の入力と備考への追加に対応する。
- 店舗施策のため、AI下書きで必要人数4名へ切り替える「異なるECモールが2つ以上」の判定には含めない。
- TSG本番Supabase `zfhswguzqyagmhhlpksq` へ migration `202607300002_shift_brand_hall_store_sale.sql` を適用し、Vercel本番へデプロイ。本番フロア画面のECセール一覧とコンソールエラー0件を確認。

## 2026-07-30 シフト印刷の備考・配色整理

- フロア・製造の「任意の備考を追加」は備考2へ保存・表示し、備考1はECセール表示用に分離。既存自由文も migration `202607300003_shift_free_notes_to_notes2.sql` で備考2へ移行した。道の駅のTimee項目と備考は従来どおり。
- 印刷表で手動色を設定していない勤務セルの自動薄緑を白へ変更。希望休のグレー、会社休の黒、セルごとの手動色は維持する。
- スタッフ名ヘッダーは、5日・6日正社員を濃い赤、パート・フルタイムパートを薄い赤で区別する。本番フロア印刷で勤務セル白、勤務形態別ヘッダー色、既存自由文の備考2表示を確認し、Vercel本番へデプロイ済み。

## 2026-07-30 ECセール時刻の片側入力

- ECセールの時刻入力後、追加ボタンがReactの更新前状態を参照して時刻を落とす問題を修正。追加時に入力欄の最新値を回収して反映する。
- 開始のみは「18:00〜」、終了のみは「〜20:00」、両方は「18:00〜20:00」として画面・印刷・Excelへ引き継ぐ。
- `pnpm lint`、`pnpm build`、Vercel本番ビルドを確認。本番画面で3パターンの表示を確認し、検証入力は保存せず破棄した。

## 2026-07-30 シフト勤務候補・印刷・確定済み削除

- 所属別の勤務候補は、追加・編集・削除・並べ替え後にシフト画面全体を再読込せず、候補一覧だけを即時更新する方式へ変更。編集中のシフトがあっても候補管理を操作でき、保存中の表示も追加した。
- シフト印刷時に全要素を非表示にしていたマニュアル印刷用CSSをマニュアル画面内だけへ限定し、シフト印刷が白紙になる問題を修正した。
- シフト表は名前付き印刷ページ `shift-landscape` で `A4横`、誓約書は `pledge-portrait` で `A4縦` として分離し、誓約書の縦指定がシフトへ上書きされないようにした。
- 確定済みシフトも削除可能に変更。公開済みシフト・希望・日別条件・勤務割当も削除され、元に戻せないことを明記した強い確認警告を表示する。
- `pnpm lint` はエラー0件、`pnpm build` とVercel本番ビルドに成功。本番で勤務候補の追加・削除、確定済み削除ボタンと警告、印刷内容表示、ブラウザの `A4 landscape` 認識を確認。検証用勤務候補は削除済み。

## 2026-07-30 製造シフトの備考分離・印刷均等分割

- 製造シフトからECセールの管理・入力・印刷・Excel出力を除外し、日別欄を「備考1」「備考2」の独立した自由入力欄へ変更した。
- 既存の製造作業予定は失わないよう備考2へ互換表示し、次回編集時に備考2データへ移行する。備考1・備考2のセル色も別々に保存できる。
- 印刷が複数ページになる場合はスタッフを均等配分する方式へ変更。現在の製造11名は6名・5名に分割し、前半・後半とも同じ並びで印刷する。
- `pnpm lint` はエラー0件、`pnpm build` とVercel本番ビルドに成功。本番の製造画面でECセール0件、備考1・備考2、印刷4ページのスタッフ数6・5・6・5を確認した。

## 2026-07-30 シフト確定のホーム通知

- 本番シフトを確定保存した時、対象所属の現役シフト要員へ1人1件のホーム通知をDBトリガーで作成する。期間除外者・退職者・TSG君・テストシフトは通知対象外。
- 通知はホーム上部に「シフトが確定しました」と表示し、「シフトを見る」で対象の確定シフトを開いて既読になる。同じ期間とユーザーの通知は一意制約で重複作成しない。
- すでに確定済みのフロア `2026-08-01〜2026-08-20` は現役6名へ通知を作成した。過去の7月シフトには遡及していない。
- migration `202607300004_shift_confirmation_alerts.sql` を本番適用。DB lint、`pnpm lint`、`pnpm build`、Vercel本番ビルドに成功。検証用通知でホーム表示・既読・シフト遷移を確認し、検証データは削除済み。

## 2026-07-30 シフト期間削除の不具合修正

- 確定済みシフトの削除時、削除処理より先に「確定後は変更不可」の判定へ入り、APIが409を返していた処理順の不整合を修正した。期間削除だけは警告確認後に実行でき、その他の確定済み編集は引き続き禁止する。
- APIは削除件数と削除対象IDを返し、対象が既に存在しない場合も明示する。画面は同じIDの削除成功を確認してから一覧を再読み込みし、成功・削除済み・再読込失敗を区別して表示する。
- `pnpm exec eslint app/api/admin/shifts/route.ts components/shift-admin-tab.tsx` はエラー0件、`pnpm build` 成功。本番で検証用下書きの削除と一覧からの消失を確認し、検証データは削除済み。Vercel本番 `DoJoibau2tQFVMRvXMrgQeK3Hwhz` を `https://v0-line-blush.vercel.app` へ反映した。

## 2026-07-31 正社員向け忌引き休暇

- 管理の既存「休暇・欠勤」内に「忌引き休暇」を追加し、5日正社員・6日正社員だけが申請できるようにした。パート・フルタイムパート・役員は申請対象外。
- 規定は1親等（父母・子）7日、2親等（祖父母・孫・兄弟姉妹）3日、3親等（曽祖父母・ひ孫・叔父叔母・甥姪）1日、4親等（いとこ）は規定なしとして画面とDB制約を一致させた。
- 申請、管理者・役員による承認／却下／取消、本人履歴、処理履歴、管理者と本人への通知を実装。通常の有給残日数とは別管理とした。
- migration `202607310001_bereavement_leave.sql` をTSG本番Supabaseへ適用。DB lint、対象ESLint、`pnpm build`、Vercel本番ビルドに成功し、PC・390pxスマホで表示と横はみ出しなしを確認した。

## 2026-07-31 有給システム開始前申請の移行

- 有給掲示板の投稿 `5c19c1d0-1db1-43f4-ad61-81f34d5c1c7c` を出典として、渡部 瞳（社員NO 66）の2026年7月30日・半休0.5日を承認済み実績として本番台帳へ反映した。
- 旧管理から移行した36.5日から0.5日を割り当て、2026年7月31日時点の有給残を投稿記載どおり36日にした。2026年8月1日の移行調整2日は変更していない。
- 申請、消化割当、承認者、掲示板原文・投稿ID、監査ログを保存した。8月1日のシフト連動開始前のため、勤怠・出勤率へは遡及させず初期残高使用として扱う。

## 2026-07-31 未打刻回答への忌引き休追加・石井瑞季の有給取消

- 確定シフトの勤務日に打刻がない場合の本人回答へ「忌引き休」を追加した。5日正社員・6日正社員だけが選択でき、パート系・役員は画面とAPIの両方で対象外。
- 管理者が忌引き休を確定しても有給残日数は消費せず、欠勤回数にも加算しない。出勤率判定では出勤相当として扱い、管理画面の回答区分にも「忌引き休」と表示する。
- 石井 瑞季（社員NO 92）が2026年7月31日に申請・承認済みだった有給全休1日を取消した。消化割当を無効化し、有給残を15日から16日に復元した。
- migration `202607310002_bereavement_workday_resolution.sql` をTSG本番Supabaseへ適用。DB lint、対象ESLint、`pnpm build`、DB登録・削除テストに成功し、検証データは削除済み。Vercel本番 `dpl_6zNM5F64bfRzAhz45h2ds9s4iTGs` を `https://v0-line-blush.vercel.app` へ反映した。

## 2026-07-31 打刻なし日の勤怠備考保持

- 管理の月次勤怠で、出勤・退勤が空欄の日は備考の保存先となる打刻行がなく、入力した「忌引き休」等が再読込時に消えていた問題を修正した。
- 日次備考をユーザー・日付単位で独立保存し、打刻の有無にかかわらず再表示する。既存の打刻付き備考は互換表示し、労務士提出Excelも日次備考を優先して出力する。
- migration `202607310003_attendance_daily_notes.sql` を本番適用。DB lint、対象ESLint、`pnpm build`、打刻なし日の「忌引き休」保存・再取得・削除テストに成功し、検証データは削除済み。Vercel本番 `dpl_FAmMgC5fsh5ba4YCVtRQpFVMwKZV` を `https://v0-line-blush.vercel.app` へ反映した。

## 2026-07-31 忌引き休の確定シフト勤務日カウント

- 忌引き休暇を開始日・終了日の暦日数で数えていた初期実装を廃止し、「開始日＋取得する勤務日数」で申請する方式へ変更した。
- 確定シフト上の勤務日だけを対象とし、希望休・会社休・元々の休日・有給日は自動で飛ばす。申請前に実際の対象勤務日と除外した休日日数を表示する。
- 対象勤務日は申請時にスナップショット保存し、承認時に勤怠の `忌引き休` として確定する。取消時は対象日の勤怠確定も無効化し、有給残と欠勤回数には影響させない。
- 管理の月次勤怠と労務士提出Excelにも承認済み対象日を「忌引き休」と表示する。8月確定シフトを使い、8/1・8/2・8/4を対象、元々休みの8/3を除外する3日申請と承認・取消を検証し、検証データは削除済み。
- migration `202607310004_bereavement_shift_workdays.sql` を本番適用し、DB lint、対象ESLint、`pnpm build` に成功。Vercel本番 `dpl_7D2Nfr7Ak9SEH2q9HbadFKXsyqHK` を `https://v0-line-blush.vercel.app` へ反映した。

## 2026-08-01 退職処理と過去勤怠の保持

- 人事管理に退職日を指定する「退職処理」を追加した。TSGアカウントは停止するが、社員・勤怠・給与・労務士提出用の履歴は削除しない。
- 勤怠と給与の対象者判定を対象月基準へ変更し、退職月までは集計対象、退職日の翌月以降は対象外とする。給与・人事に紐づくユーザーの物理削除は禁止した。
- 山口 侑哉を2026-07-31付で退職処理した。7月の勤怠・給与データを保持し、8月以降の在籍対象とログインから除外した。

## 2026-08-01 管理者本人の有給申請

- 管理者権限のスタッフで「全員管理」だけが表示され、本人の有給申請ができなかった画面分岐を修正した。
- 有給対象の管理者には「自分の申請」「全員管理」の切替を表示し、初期表示は「自分の申請」とした。役員など有給対象外のアカウントは全員管理のみ表示する。
- `pnpm exec eslint app/admin/page.tsx` と `pnpm build` に成功。Vercel本番へ反映し、`https://v0-line-blush.vercel.app` の管理画面更新を確認した。

## 2026-08-01 全社員ホームメッセージ

- 佐藤 正彦だけが、掲示板・グループChatとは別にTSGホームへ全社員メッセージを送信できる機能を追加した。本文と画像1点を送信できる。
- 配信対象は送信時点の在籍社員に固定し、佐藤 正彦本人を含む。TSG君・退職者は除外し、後日入社したスタッフへ過去メッセージが遡及表示されない仕様とした。
- 各スタッフの「表示しない」は本人のホームだけを非表示にし、他スタッフの表示や原文は削除しない。送信権限は機能ロールで佐藤 正彦1名に限定した。
- 本番DB適用、DB lint、対象ESLint、`pnpm build`、24名配信・本人包含・個別非表示のDB/API検証に成功。Vercel本番へ反映し、PC・390pxスマホで送信画面、画像指定、横はみ出しなし、ブラウザエラー0件を確認した。

## 2026-08-01 全社員メッセージの送信・受信画面分離

- 佐藤 正彦専用の送信画面だけをDM一覧の最上部へ配置し、受信した本文・画像は各受信者のホームだけに表示する構成へ整理した。
- 佐藤 正彦本人も配信対象に含め、送信後は本人のホームにも同じメッセージを表示する。DMには受信メッセージを表示しない。
- 画像添付、送信時点の在籍者固定、各自の「表示しない」による個別非表示ロジックは変更していない。
- 対象ESLintはエラー0件、ローカル・Vercel本番ビルドに成功。本番の佐藤 正彦アカウントでDMの送信欄あり・受信カード0件、ホームの送信欄なし・受信カード1件を確認した。

## 2026-08-01 有給申請の承認者限定

- 役員・管理者全員が承認できていた有給本人申請を、佐藤 正彦だけが承認・却下できる仕様へ変更した。
- 申請時のプッシュ通知、ホームの承認待ち表示、管理画面の承認・却下ボタン、承認APIを同じ専用権限で制御した。他の管理者は履歴確認のみ可能。
- 専用権限を本番DBへ1名分登録し、DB lint、対象ESLint、`pnpm build`、Vercel本番ビルドに成功。本番の佐藤 正彦アカウントで承認者表示とブラウザエラー0件を確認した。

## 2026-08-01 リアクションアイコン統一

- 掲示板、掲示板コメント、グループChat、DMのリアクションを共通定義へまとめ、`👍 ❤️ 😂 😮 😢 😡` の6種類・同一順序に統一した。
- リアクションの保存・取消・通知ロジックは変更していない。
- 対象ESLintはエラー0件、ローカル・Vercel本番ビルドに成功。本番の掲示板とChatで同一6種類を確認し、ブラウザエラー0件を確認した。

## 2026-08-03 有給承認の管理職掲示板連携

- 有給本人申請を佐藤 正彦が承認した時点で、TSGくんが管理職掲示板へ承認内容を自動投稿するようにした。投稿には申請者、所属、取得日、全休・半休、理由・補足、承認者、承認日時を記載する。
- 有給残の引当、承認、掲示板投稿を同一DBトランザクションで処理し、申請データに投稿IDを保存する。途中失敗で承認だけが残る状態と、再操作による二重投稿を防止した。
- 管理職掲示板メンバーへのプッシュ通知と、申請者本人への既存の承認通知を維持した。migration `202608030001_paid_leave_management_board_post.sql` をTSG本番Supabaseへ適用し、DB lint、対象ESLint、ローカル・Vercel本番ビルドに成功。Vercel本番 `dpl_8N4XzfVneqbBSgHQBxzNRFyRNNac` を `https://v0-line-blush.vercel.app` へ反映した。
- 連携開始前に承認済みだった今月分1件（藤田 香織、2026年8月2日、有給全休）をTSG君名義で管理職掲示板へ遡及投稿した。今月分の承認済み未投稿は0件になった。

## 2026-08-03 雇用保険加入の個別案内

- 規定勤務時間到達により2026年7月1日付で雇用保険へ加入し、2026年8月10日支給分から雇用保険料を控除する案内を、TSG君から3名へ個別DM送信した。
- 送信対象は佐藤 葵（フロア・女性・社員NO146）、小桧山 優樹（社員NO144）、舟木 睦美（社員NO143）。佐藤 葵（製造・男性・社員NO147）は対象外。
- 本文に「不明点がある場合は社長まで確認」を記載し、3件とも正しい送信者・送信先・本文・未読状態を本番DBで確認した。
- 佐藤 ちさとへも、対象3名、7月1日付加入、8月10日支給分からの保険料控除、本人3名へ案内済みであることをTSG君から個別DM送信した。新規DMはTSG君と佐藤 ちさとの2名だけで構成し、未読状態を確認した。

## 2026-08-03 勤怠・労務士提出の対象者整理

- 勤怠管理の対象からTSG君、佐藤 正彦、佐藤 ちさとの3名を除外した。過去の勤怠・給与履歴は削除せず保持する。
- 月次一覧、まとめて保存、チェック完了判定、部門別Excel、労務士メール添付の対象判定を共通化し、3名が未チェック扱いや提出シートに入らないようにした。
- 同姓の佐藤 葵は対象のままであることを確認。2026年8月の対象人数はフロア6名、製造11名、道の駅4名。対象ESLintはエラー0件、ローカル・Vercel本番ビルドに成功し、本番 `dpl_GNLfKU6yLiGej9He2Hr6Pf2nRiGa` を反映した。

## 2026-08-03 労務士提出への新入社員資料添付

- 月末勤怠の労務士提出欄へ「新入社員情報」の添付ボタンを追加した。PDF・画像・Word・Excel・CSV・TXT・ZIPを最大6件、合計3.5MBまで複数選択でき、送信前にファイル名・容量を確認して個別に外せる。
- 追加資料は既存のフロア・製造・道の駅の勤怠Excelと同じGmailへ添付し、メール本文にも資料名を記載する。個人情報を公開Driveへ保存せず、送信時だけ取り扱う。失敗時は選択を保持し、送信成功後だけ添付欄を空にする。
- 画面とAPIの双方で件数・合計容量・拡張子を検査する。対象ESLintはエラー0件、ローカル・Vercel本番ビルドに成功。本番 `dpl_5Y4exT7vtyvzRnFGGUFFMUUkQLfq` を反映し、PC・390pxスマホで表示、複数選択設定、横はみ出しなし、ブラウザエラー0件を確認した。検証では労務士への実送信は行っていない。

## 2026-08-03 DocScanner新入社員資料の労務士提出連携

- 労務士提出の新入社員情報は、DocScannerから人事管理へ取り込まれた履歴書PDFを主経路に変更した。対象月の入社者を上位表示し、氏名・所属・社員NO・入社日・原稿名・容量を確認してチェック選択できる。手元ファイル添付は補助欄として残した。
- 選択した履歴書は非公開Supabase Storageからサーバー内で取得し、公開URLや端末への再保存を介さず、3所属の勤怠Excelと同じGmailへ実ファイル添付する。最大6件・合計12MBとし、最新版・DocScanner由来・在籍者の書類だけを送信できるようサーバー側でも再検証する。
- 本番 `dpl_5A7GnghW4einJNsCN6hn6DQZJ2kd` を反映。現在のDocScanner取込候補として新田 奈美の履歴書1件が表示され、選択状態、PC・390pxスマホ表示、横はみ出しなし、ブラウザエラー0件を確認した。対象ESLintはエラー0件、ローカル・Vercel本番ビルド成功。検証では労務士への実送信は行っていない。

## 2026-08-03 修正内容の個別案内

- 勤怠対象者の整理、新入社員資料添付、DocScanner履歴書連携の運用ポイントを佐藤ちさとへ個別DMで案内した。
- TSG君・佐藤正彦・佐藤ちさとを勤怠管理と労務士提出対象から除外したこと、DocScanner候補を選択すると勤怠Excelと同じメールへ履歴書PDFを添付すること、新田奈美の履歴書が現在の候補であることを記載した。

## 2026-08-04 DocScanner資料の書類一覧選択化

- 勤怠管理を開いた時のDocScanner資料自動取得・自動展開を廃止し、「書類一覧から選択」を押した時だけ一覧を取得する方式へ変更した。
- 検索、複数チェック、選択確定、確定後の資料一覧と個別解除を追加した。手元ファイルのアップロード機能と、送信時の非公開PDF取得・サーバー再検証は維持した。
- 本番 `dpl_AxSASP2wGN9pLqtC8iCHhPWCxuuP` を反映。PC・390pxスマホで一覧表示と選択確定を確認し、横切れ・ブラウザエラーは0件。労務士への実送信は行っていない。

## 2026-08-04 DocScanner全書類一覧と選択連携

- 新入社員資料の選択元を履歴書候補だけでなく、DocScannerの最新100件へ拡張した。氏名がない書類もファイル名・日付・取引先・概要で確認・絞り込みできる。
- HTTPSのTSGから社内HTTPへ直接接続できない制約を避けるため、DocScannerのローカル選択専用画面を開き、選択した実ファイルだけをTSGへ戻す方式にした。未選択書類はTSGへ送らない。
- 選択画面の下部操作を固定し、キャンセルと選択確定が画面幅内へ収まるよう調整。本番 `dpl_AQRnVp2PvyQYii9Qk4rqm8tsqWcT` を反映し、100件表示、履歴書選択、TSGへの受渡し、解除まで確認した。労務士への実送信は行っていない。

## 2026-08-04 DocScanner別PC接続修正

- 書類選択画面が `127.0.0.1:3004` を優先していたため、DocScannerを動かしていない別PCでは自端末へ接続して失敗していた。
- DocScanner端末の社内LANアドレス `192.168.110.200:3004` を優先し、同端末でのみループバックへフォールバックする順序へ変更した。3004番ポートは全インターフェースで待受中、Windowsファイアウォール許可済み、LAN URLのHTTP 200を確認した。
- Vercel本番 `dpl_FUCn4aKY1y2M9FjQPX4mYRuZyUPm` を `https://v0-line-blush.vercel.app` へ反映。本番の「書類一覧から選択」からLAN URLが開くことを確認した。

## 2026-08-07 労務士ZIPの給与結果解析・比較復旧

- 労務士ZIPを登録するだけで社員別給与結果がDBへ入らず、比較が0人になる問題を修正した。ZIP内の支給控除一覧表と賃金台帳Excelを優先解析し、社員別給与結果・支給控除明細を自動登録する。
- 書込み前に社員別合計と支給控除一覧表の全社計を照合し、不一致時はDBを更新しない。2026年7月勤務分・8月10日支給は24名、支給3,855,731円、控除596,304円、差引3,259,427円、明細215件で原本全社計と一致した。
- 社員NO、旧姓・通名、括弧付き同名を使って突合するようにした。森結芽香/鈴木結芽香、生井美穂/内海美穂、佐藤葵（フロア）/佐藤葵（女性）、佐藤葵（製造）/佐藤葵（男性）を同一人物として確定した。
- 解析済みZIPにも修正後ロジックを再適用できる「ZIPを再解析」を追加した。本番 `https://v0-line-blush.vercel.app` で再解析し、比較可能24/24名・100%、全24名の社員別表示、未突合警告0件を確認した。残る表示は取込不足ではなく、社内計算と労務士確定額の実差異である。

## 2026-08-07 Googleカレンダー自動同期の認証復旧

- カレンダーに「DBマイグレーション未適用」と表示されたが、本番DBを直接診断し、Google取込用の列と同期状態テーブルはいずれも適用済みであることを確認した。
- 実原因はVercel本番の `GOOGLE_SERVICE_ACCOUNT_KEY` に余分な末尾文字が入り、サービスアカウントJSONを解析できなかったことだった。JSONを正規化してVercel本番環境へ再登録した。
- 認証JSONの外側に誤った引用符・改行が残ってもJSON本体を復元するようパーサーを補強した。また、JSON解析エラーの `column` 表記をDB列不足と誤判定しないようエラー分類を限定した。
- 一時診断API・画面は削除して本番へ反映。本番カレンダーで2026年8月54件（色50件）、9月47件（色45件）の自動同期を確認し、DBエラー表示なし、ブラウザエラー0件を確認した。

## 2026-08-08 Chat本文のURLリンク化

- グループChat・DMの本文表示に、掲示板と共通の安全なURLリンク化処理を適用した。`http://` / `https://` を別タブで開き、URL末尾の句読点はリンク対象から除外する。
- 対象ファイルのESLintと本番ビルドに成功し、Vercel本番へ反映した。本番の管理職Chatで既存URLがリンク表示され、クリック先・別タブ指定・ブラウザエラー0件を確認した。
- リポジトリ全体の型確認には別機能の既存エラー3件が残るが、今回変更したChatと共通リンク処理には型エラーなし。

## 2026-08-09 給与・労務の統合と過去給与再検証

- 管理画面の「労務データ」と「給与計算」を「給与・労務」へ統合し、「取込・照合」と「確定給与・人件費」を画面内で切り替える構成へ整理した。原文ファイルは折りたたみ、通常は検証結果と変更点を先に表示する。
- 2025年8月支給分から2026年6月支給分までの月例給与11か月・257社員明細をPDF原票から再解析した。社員別支給・控除・手取の保存値を補正し、全11か月で全社合計と `支給 - 控除 = 手取` の一致を確認した。
- 過去月の基本給・時給・残業単価・通勤費・控除を月別計算プロファイルとして保持するようにした。最低賃金切替月の表示単価と実適用単価の差、その他控除合計の二重加算、佐藤葵2名、森結芽香/鈴木結芽香、生井美穂/内海美穂の氏名差を補正した。
- 保存済み月例給与11か月は、各月の比較対象者全員で支給・控除・手取差0円。2026年7月勤務・8月10日支給分も労務士勤怠を使った給与式は24/24名・支給差0円。TSG実打刻との差がある22名は「勤怠差」として給与式の差から分離した。
- 2026年6月勤務・7月10日支給分は確定原文が解析待ちで、現在は比較不能として明示する。賞与は月例給与式の比較対象から外し、確定給与・人件費側で実績参照する。
- `pnpm build` とVercel本番ビルドに成功。`pnpm exec tsc --noEmit` は今回変更部分のエラー0件で、シフト印刷・誓約PDF・Supabase管理クライアントの既存3件のみ残る。
- 給与・労務を開いた時だけPC向け作業幅へ切り替え、取込・照合と確定給与・人件費を横幅を使って確認できるようにした。スマホ中心の他の管理画面の幅は変更していない。

## 2026-08-11 グループChatの既読者アイコン

- 自分が送ったグループChatの「既読」欄へ、既読者のLINE写真を丸アイコンで表示するようにした。表示幅を抑えるため直近3人を重ね、4人目以降は `+人数` へまとめる。
- PCでは写真へのカーソル移動、スマホでは写真のタップで氏名を表示する。`+人数` では省略された既読者全員の氏名を確認できる。既読時刻の判定ロジックは変更していない。
- 対象ESLint、差分チェック、ローカル・Vercel本番ビルドに成功。本番の管理職Chatで既読者1～3人の写真・氏名を確認し、390px幅で横はみ出しなし、ブラウザエラー0件を確認した。本番 `BkeGnXcx2HmpDdw6WdVhRfKYDCfu` を `https://v0-line-blush.vercel.app` へ反映した。

## 2026-08-11 掲示板の既読者アイコン

- 掲示板でも自分の通常投稿とコメントに、既読者のLINE写真を丸アイコンで表示するようにした。Chatと同じく直近3人と `+人数` で表示し、PCのカーソル移動・スマホのタップで氏名を確認できる。
- Chatと掲示板の既読表示を共通コンポーネント化した。既存のグループ別最終閲覧時刻による既読判定は変更していない。
- 対象ESLintはエラー0件、差分チェック、ローカル・Vercel本番ビルドに成功。本番掲示板で投稿の既読者12～15人、コメントの既読表示、390px幅で横はみ出しなし、ブラウザエラー0件を確認した。本番 `FgS6u8HXevv1g4ZkjKWn5TJkg1Fp` を `https://v0-line-blush.vercel.app` へ反映した。

## 2026-08-11 確定シフトのPC全体表示

- 確定シフト閲覧画面は、PC幅で720pxに制限されていたページを表示領域全体へ広げ、人数に応じて指定されていた表の最小幅をPC時だけ解除した。日付・全スタッフ・人数・備考を画面内へ再配分し、横スクロールバーを表示しない。
- スマホでは既存のExcel形式の列幅と表内横スクロールを維持した。本番実データでフロアと製造16列を確認し、1920px幅は表内・ページ全体とも横はみ出しなし、390px幅は表内横スクロールあり・ページ全体の横はみ出しなし、ブラウザエラー0件を確認した。
- 差分チェック、ローカル・Vercel本番ビルドに成功。本番 `7cRGM2xuYJPUnHPrdP8XDdXBpWXY` を `https://v0-line-blush.vercel.app` へ反映した。

## 2026-08-11 全社員メッセージのDM履歴

- DM一覧の上部へ、ログイン済み全員が閲覧できる全社員メッセージ履歴を追加した。配信日とタイトルを一覧表示し、行を押すと本文と添付画像を展開する。
- ホームの「表示しない」は従来どおり個人別のホーム表示だけを終了し、DM履歴はメッセージ本体から取得するため消えない。後から登録された在籍ユーザーも過去の全社員メッセージを確認できる。
- 新規送信画面へタイトル欄を追加し、既存配信2件は本文先頭行からタイトルを補完した。DBマイグレーション `202608110001_home_company_message_titles.sql` を本番へ適用した。
- 対象ESLint、差分チェック、ローカル・Vercel本番ビルドに成功。本番DMで履歴2件、本文展開、新規送信欄、390px幅の横はみ出しなし、ブラウザエラー0件を確認した。本番 `79bHBMBfjYG7PfpfY4o1WRnSdDbk` を `https://v0-line-blush.vercel.app` へ反映した。

## 2026-08-13 佐藤葵2名の本人対応修正

- 労務士資料を性別・生年月日・入社日・旧社員NOで再照合した。資料自体は、旧社員NO141が女性・入社日2025-12-11、旧社員NO125が男性・入社日2024-12-17で一貫していた。TSG取込時の同姓同名対応が逆転していたことが原因。
- 現社員NO146の佐藤 葵（フロア）を女性・5日正社員・入社日2025-12-11・旧社員NO141へ、現社員NO147の佐藤 葵（製造）を男性・パート・入社日2024-12-17・旧社員NO125へ統一した。
- 人事マスタだけでなく、過去の労務士給与結果、月別給与計算プロファイル、氏名別名、8月1日付有給、有給次回付与、再構築用初期投入SQL、給与突合の既知対応表を一括修正した。DBマイグレーション `202608130001_correct_aoi_identity_mapping.sql` は最終整合性検査付きの単一トランザクションで本番適用済み。
- 本番でフロアは有給10日・次回2027-06-11に11日、製造は有給8日・次回2027-06-17に9日を確認した。2026年7月勤務分の給与突合は24/24人・支給差0円で、両名とも正しい社員NOと所属へ表示されることを確認した。
- 対象ESLint、差分チェック、ローカル・Vercel本番ビルドに成功。本番 `3P8gREcHccJpD3BCF8j1VXmyFonz` を `https://v0-line-blush.vercel.app` へ反映した。全体 `tsc --noEmit` には今回と無関係な既存3件（シフト印刷、誓約PDF、Supabase管理クライアント型）が残る。

## 2026-08-13 佐藤葵2名の訂正完了DM

- 藤田香織さんへ、佐藤葵2名の本人対応修正が完了した旨をTSG君名義の個別DMで送信した。
- フロアは女性・5日正社員・入社日2025-12-11、製造は男性・パート・入社日2024-12-17であること、人事・有給・過去給与・今後の労務データ取込設定まで修正し、給与照合の全員一致を確認済みであることを記載した。
- 本番マイグレーション `202608130002_notify_fujita_aoi_correction.sql` で送信者・送信先を氏名と在籍状態から一意特定し、2名だけのDMメンバー構成、TSG君の投稿者ID、本文一致を検証した。再適用時は同一本文を重複送信しない。

## 2026-08-14 芳賀・本名の有給残数差の確認

- 本番の有給管理で、芳賀加南子さん26日、本名英子さん12日を確認した。両名とも2026-08-01の「制度開始時みなし付与」は12日であり、既存正社員4名にだけ付けた会社独自の「移行調整休暇」ではない。
- 芳賀さんは入社日2023-02-13のため、入社日基準の通常の年次付与日2026-08-13に14日が追加され、12日+14日=26日になっている。本名さんは入社日2023-09-11で、次回の通常付与は2027-03-11の14日。
- 現行実装は「制度開始時みなし付与」「既存正社員の移行調整休暇」「入社日基準の通常付与」を別ロットで管理する。芳賀さんと本名さんの差は通常付与日の到来時期によるもので、移行調整休暇や利用履歴による差ではない。

## 2026-08-14 シフト表示・有給申請経路の整合

- シフト作成画面と確定シフト閲覧画面で、保存済みのセル背景色が希望休・会社休・入力済み・ライトテーマの既定色に上書きされないよう表示優先順位を統一した。本番の確定シフトで保存色19セル、作成画面で保存色7セルの実背景色を確認した。
- 有給承認を、一般スタッフは同じ所属の管理者、管理者・役員本人は佐藤正彦へ送る経路に変更した。TSG君は管理者設定でも承認先から除外し、所属管理者が未設定の場合だけ佐藤正彦へフォールバックする。佐藤正彦は承認待ちを引き受けず、管理画面から全スタッフの代理登録はできる。
- 希望提出期限後もシフト確定前であれば、本人は同じ希望提出画面から有給（全休・半休）だけ修正可能とした。管理者も作成画面の各セルで全休・半休を設定でき、確定後は管理の有給画面から確定勤務日へ代理登録する。
- 希望回収で登録した全休有給は、確定シフトと印刷では会社指定休と同じ黒セル・「休」で表示し、有給の文字と勤務人数から除外する。内部の有給申請・残日数消費・賃金スナップショットは保持し、労務士提出時は承認済み有給をシステム打刻へ変換する。半休は勤務シフトを表示し、実勤務分は通常打刻する。
- 対象ESLint、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功。本番デプロイ `4PYUdqk9TiheBTJUvy1RN9KyuGYZ` を `https://v0-line-blush.vercel.app` へ反映し、作成画面・確定シフト・有給管理画面でエラーなしを確認した。

## 2026-08-14 菅山紗来さんの仮入社・履歴書取込

- DocScannerの履歴書をTSG人事管理へ取り込み、社員NO149、フロア、女性、パート（週29.5時間以内）、入社日2026-08-24の仮入社として登録した。履歴書PDFとAI読取結果は人事情報へ保存済み。
- LINE登録前でもシフト表へ表示できる仮アカウントを内部生成した。入社日前の勤務はAI下書き・手動保存とも割り当て不可とし、本人向け希望回収・通知・通常ユーザー管理には出さない。
- 入社後に同名のLINEユーザーが承認された場合は、仮入社の人事情報・社員NO・シフトを実アカウントへ自動引継ぎする。仮アカウントは引継ぎ後に削除する。
- 対象ESLintはエラー0件、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功。本番人事管理で仮入社表示・履歴書読取済みを確認し、シフト画面で候補表示・希望回収の未提出表示なしを確認した。本番 `95HnP5HdJM69kS3dqhUS4Dvf3vwj` を `https://v0-line-blush.vercel.app` へ反映した。
- 作業完了後、藤田香織さんとのDMへ、仮入社条件・履歴書取込・シフト表示・LINE登録後の自動引継ぎについて完了報告を送信し、投稿表示を確認した。

## 2026-08-16 道の駅シフト希望回収の配信

- 作成済みシフトにも希望締切の変更欄を追加し、期間を作り直さず締切日を更新できるようにした。確定済み期間では従来どおり変更不可。
- 道の駅2026-08-21〜2026-08-31の希望締切を2026-08-17へ変更し、佐藤ちさと・生井美穂・武藤志保・角田聖子・新田奈美の5名へ希望回収を配信した。
- 対象期間が「希望回収中」、対象5名が全員「未提出」へ切り替わったことを本番で確認した。対象ESLint、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功。本番 `FXfwNtSuaF1kBhUxHiJXFhwTRpUT` を `https://v0-line-blush.vercel.app` へ反映した。

## 2026-08-16 仮入社シフト保存・有給プレビュー反映

- 菅山紗来さんの入社日前（2026-08-21〜23）まで勤務入力でき、1件でも含むと期間全体の保存が拒否される状態だった。入社日前セルを「入社前」と明示して勤務・有給・会社休・時刻入力を無効化し、入社日2026-08-24以降だけ編集可能にした。
- シフト作成画面の未保存変更はブラウザ内だけにあり、従来の印刷プレビューは保存済みDBを直接開いていた。印刷プレビューをボタン化し、未保存変更がある場合は一時保存と再読込が成功してからプレビューへ遷移するよう統一した。これにより有給（全休）は会社休と同じ黒セルで即時確認できる。
- 本番で菅山さんの8月24日勤務を保存・再読込・プレビュー反映まで確認した。森結芽香さんの8月21日を全休へ変更し、保存後のプレビューで `shift-print-cell--company-off` の黒セル・勤務人数除外を確認した。検証用変更は双方とも元の勤務状態へ復元済み。
- 対象ESLintはエラー0件（既存の未使用関数警告1件のみ）、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功。本番 `DA2u2o1u8oNjSSnBiHHbt6KRYRQU` を `https://v0-line-blush.vercel.app` へ反映した。
- 作業完了後、藤田香織さんへ、2件の修正内容・本番検証完了・検証用変更の復元済みを個別DMで報告し、送信後の本文表示を確認した。

## 2026-08-16 会津ブランド館小口管理の完了報告

- DocScannerの会津ブランド館小口現金単独運用について、入金、レシートOCRによる現金支出反映、月次一覧・Excel・印刷、会計事務所提出用白台紙の実装内容と2026年8月実データの照合結果を、TSG君から藤田香織さんへ個別DMで報告した。
- その後の運用変更として、会津ブランド館と製造を同一残高・同一月次明細で管理し、道の駅のみ別管理としたことを追加報告した。製造の過去入金が未登録で、既存データ合算残高が `-21,504円` となる点も、未確認の残高補正を行わず確認事項として明記した。
- 宛先・送信者の一意確認、個人DMの2名構成、同一本文の重複防止を含む本番マイグレーション `202608160101` を適用し、ローカル・リモート双方への反映を確認した。

## 2026-08-18 スマホ確定シフト最終行の再修正

- 前回の `100dvh - 410px` による固定高計算では、実端末の表示高・セーフエリア・表示期間欄の有無によって確定シフト表が固定下メニューへ潜る余地が残っていたため、固定値方式を廃止した。
- 確定シフト表示中は、上部ヘッダーと固定下メニューを除いた残りの画面高をページへ割り当て、タブ・見出し・部署・期間・凡例の実際の高さを除いた残りを表スクロール領域へ自動配分する構成に変更した。最終行下にも12pxのスクロール余白を設けた。
- 高さ620px以下の短い縦画面では、重複する確定シフト大見出しと色凡例を省略して表の行表示を優先する。横向き全画面表示と表内横スクロールは維持した。
- 対象ESLint、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功。本番 `7Q6x5BwLDLFCwUBaZ9HNPqr2myAt` を `https://v0-line-blush.vercel.app` へ反映し、`390x844`、`390x667`、`320x568`、横向き `844x390` で8月20日が下メニューより上に完全表示されること、ブラウザエラー0件を確認した。

## 2026-08-19 ホーム画面版の完成シフト横向き対応

- ホーム画面へ追加したPWAのmanifestが `portrait-primary` で縦向き固定になっていたため、横向き用CSSがあってもstandalone起動時に画面回転できない状態だった。manifestの向きを `any` へ変更し、参照URLへ更新識別子を付けて古いmanifestキャッシュを回避した。
- 完成シフトを横向きにした際は、上部ヘッダーと固定下メニューを外し、部署・表示期間・Excel形式のシフト表だけを全画面表示する。表の高さは残り領域へ自動配分し、iPhoneのノッチ、左右セーフエリア、ホームインジケータも避けるようにした。
- 対象ESLint、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功。本番 `A9RRmmj1mEwxaj6meqfNBXbDWQ9r` を `https://v0-line-blush.vercel.app` へ反映した。`844x390` でページ全体390px、表296px、8月20日の最終行下端368px、ヘッダー・下メニュー非表示、ブラウザエラー0件を確認し、本番manifestも `orientation=any` を確認した。

## 2026-08-19 開発担当一本化・差分監査

- TSA・TSG・DocScannerを今後このCodexで一元管理する方針に変更し、3システムの共有開発コンテキストを同期した。
- TSGで別Codexが本日変更した範囲は、完成シフトのスマホ横向き対応に伴う `app/globals.css`、`app/layout.tsx`、`public/manifest.json`。既読保存・未読集計の `lib/read-status.ts`、`lib/unread.ts` には本日の変更がなく、既読数異常とは別件であることを確認した。
- 作業ツリーには既存開発分として変更58ファイル・未追跡239ファイルが残る。今後は作業開始時に対象ファイルの差分と更新日時を確認し、未知の変更を維持したまま機能単位で編集する。本番URLはHTTP 200を確認した。

## 2026-08-19 未読件数の全履歴化・画面残留を修正

- `gw_read_status` の取得エラーを空データとして扱っていたため、一時的なSupabase読取失敗時に所属グループの開設以来の投稿数が未読件数になる問題を修正した。取得失敗時は未読0へ安全側に倒し、既読行がない場合はグループ加入日時を集計開始基準にする。
- `/api/groups` と `/api/unread` を動的・private no-store応答へ変更した。ホームは初回だけでなく、60秒間隔、画面復帰、再表示、独自の未読更新イベントでグループ一覧を再取得し、古い応答が新しい結果を上書きしないよう要求番号で制御した。
- 既読保存処理のDBエラーを呼出元へ返し、掲示板・Chat側でログ検知できるようにした。未読バッジは実数を保持しつつ表示上限を `99+` とした。
- 本番DB監査では、過去投稿が多い掲示板を含め現在の未読が正常範囲であることを確認した。ログイン済み本番ホームを再読込し、表示中の未読は1件のグループが3つだけで、報告された116・1600・239等の巨大件数が消えていることを確認した。
- 対象ESLintはエラー0件（既存警告4件のみ）、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功。本番 `8NMAdjtTTw8ytN69eBvRrER8s79H` を `https://v0-line-blush.vercel.app` へ反映した。スマホ横画面対応の `app/globals.css`・manifest変更は維持しており、本件とは独立している。

## 2026-08-19 TSG全体復旧監査

- ローカル作業ツリーを最新版として、既読・打刻の直近修正を保持したまま、認証、ホーム、掲示板、Chat、DM、通知、管理、勤怠、タイムレコーダー、シフト、有給、カレンダー、人事、給与・労務、誓約、マニュアルを本番ログイン状態で横断確認した。PC・390pxスマホ・844x390横画面でページ全体の横はみ出し、固定メニューとの重なり、ブラウザエラーは再現しなかった。
- `eslint` はエラー0件、`tsc --noEmit` と本番ビルドは成功。本番デプロイ `dpl_6QDuqktTmNLukbj5EnDDMrEGFVyZ` を `https://v0-line-blush.vercel.app` の最新エイリアスへ再固定し、主要画面アクセス時のVercel実行時エラー0件を確認した。掲示板の初回API応答は実測約2.1秒で、読み込み不能ではない。
- 本番DBはPostgres lint警告0件。ローカル・本番ともマイグレーション71件、最終 `202608160103` で一致した。給与・労務の2026年7月勤務分は比較24/24名、主要項目24名一致、未解決0名、支給差0円を再確認した。
- 読み取り専用の中核整合性監査を追加し、有給同期、割当、スナップショット、全休有給と実打刻の衝突、シフト期間重複、社員・給与プロファイル連携、グループ所属孤児はいずれも0件だった。分割勤務5日中3日は正しい複数出退勤ペア、残る過去2日は最終退勤がないため、給与試算から自動除外され管理警告・労務士提出備考に要確認として出ることを確認した。履歴を推測で補正しない。
- Vercel本番の認証・Supabase・LINE・Google Drive・Google Calendar/Gmail・通知・連携用環境変数が登録済みであること、セッションが利用中は30日延長されること、Googleカレンダー自動同期がエラーなく完了することを確認した。

## 2026-08-19 TSA・DocScanner連携認証の復旧

- DocScannerのEC速報で投稿者選択が `Unauthorized` になった原因は、TSG受信側とTSA・DocScanner送信側の `TSG_INTEGRATION_SECRET` が一致していなかったことだった。TSGのAPI認証、対象グループ、所属者抽出ロジックに欠損はなかった。
- 秘密値を画面やログへ出さず、引用符とBOMを除去してVercel productionへ登録する `scripts/sync-tsg-integration-secret.ps1` を追加した。本番 `https://v0-line-blush.vercel.app` へ反映済み。
- DocScannerローカルとTSA本番の両方から投稿者APIを呼び、200応答と投稿者10名を確認した。存在しない投稿者での試験POSTはグループ所属検査の403まで到達し、認証通過後に不正投稿を拒否することも確認した。

## 2026-08-19 掲示板コメント返信の復旧

- APIに残っていたコメント返信の明示的な拒否だけでなく、既存のコメント件数・最新5件表示を維持できるよう、元投稿を示す `parent_id` とは別に直前の返信先を示す `reply_to_id` を追加した。
- 各コメントへ「返信」を追加し、返信先の氏名表示、解除、返信への再返信に対応した。返信先本人には専用通知、その他の所属者には従来のコメント通知を送り、メンション通知との重複は避ける。
- Supabaseマイグレーション `202608190001_board_comment_replies.sql` を本番適用。対象ESLintはエラー0件（既存警告1件）、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功した。本番 `dpl_8jfmZqvKeiwFGzugdWmDquTohNPN` を `https://v0-line-blush.vercel.app` へ反映し、ログイン済み掲示板で返信先選択と入力欄表示、ブラウザエラー0件を確認した。

## 2026-08-20 管理メニュー上部の整理

- 権限凡例と管理機能メニューを一体化し、機能名を左、利用可能な権限を小型アイコンで右へ揃えた2列メニューへ変更した。選択中は背景と左アクセントで判別でき、従来の縦方向の間延びを解消した。
- 権限判定と各管理機能の処理は変更していない。対象ESLintはエラー0件（既存警告5件）、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功。本番 `dpl_cyDswe5Z4ckjLHKkyNZunndx4kwz` を `https://v0-line-blush.vercel.app` へ反映し、ログイン済み管理画面で全9機能、権限表示、選択状態を確認した。

## 2026-08-20 TSAレシピ価格変更報告の事故監査・安全復旧

- 2026-08-19の価格変更報告実装時、実運用中の機能を多数含むローカルTSG正本ではなく、2026-06-06相当の不完全なGitHub `origin/main` を基にした変更を本番へデプロイしたことが、TSG全体の機能欠落・未読挙動異常を招いた直接原因だった。通知APIの投稿処理そのものではなく、不完全な作業ツリーで本番全体を置き換えたデプロイ事故である。
- 全体復旧後の本番には `/api/integrations/tsa/recipe-price-change` が存在せず404となっていたため、正本 `C:\作業用\ts-groupware` に受信APIと投稿整形処理の2ファイルだけを復元した。投稿IDはTSAの価格改定履歴IDから決定的に生成し、再試行時は既存投稿を返して二重投稿しない。
- 対象ESLint、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功し、全78ルートと既存の管理・シフト・打刻・DocScanner/TSA連携ルートを保持した。本番 `dpl_2yeCDFMxftP5djdvqsQYnQrrv38Y` を `https://v0-line-blush.vercel.app` へ反映した。
- 本番GETで対象掲示板 `NEWブランド館（フロア）`、投稿者 `TSG君` を確認した。既存の価格変更履歴を同じ連携IDで再送し、HTTP 200・`duplicate: true` となり、新規投稿を増やさず冪等性を確認した。TSA側の価格報告履歴は投稿済み2件、対象外1件で、未送信滞留は0件だった。
- 当時のGitHub `origin/main` は本番正本より不完全だった。2026-08-24の緊急復旧で現行機能をGitHubへ統合して解消し、以後はGitHub `main`を唯一の正本とする。旧ローカルcloneは参照専用とし、デプロイ前に全体ビルドとルート在庫を確認する。

## 2026-08-25 確定シフト基準の遅刻・早退判定

- 2026-08-25以降の確定シフトと実打刻を、既存給与計算と同じ15分丸め設定で比較し、遅刻・早退の回数と分数を月・年・運用開始後で集計する共通ロジックを追加した。過去勤務へ遡って誤アラートを発生させず、従来の打刻なし確認は2026-08-01以降を維持する。
- 5日正社員・6日正社員は、遅刻・早退・打刻不足があると本人画面とホームに確認を表示する。遅刻・早退は「欠勤」「有給（半休・0.5日）」「管理者に連絡」から回答し、所属長（管理者本人は佐藤正彦）が承認する。遅出と退勤時刻を同じだけ後ろへ移す等の承認済み変更は、遅刻・早退回数から除外する。
- パート・フルタイムパートは従来どおり実打刻の正味時間で給与計算し、遅刻・早退は回答を求めず回数・分数だけ記録する。打刻不足は給与時間を確定できないため従来どおり本人確認と管理者承認を残す。
- 有給半休・欠勤・勤務時間変更の確定結果は、既存の有給残、欠勤回数、出勤率、労務士提出処理へ接続した。正社員月給の遅刻・欠勤控除式は労務士の確定ルールが未確定のため推測実装せず、差異検証で式を確定してから給与計算へ追加する。
- 本番データ監査では8月25日以降の確定勤務107件を確認し、通常勤務で比較時刻が欠ける行は0件だった。単体テスト、既存未読テスト、ESLint（エラー0件）、`tsc --noEmit`、全79ルートの本番ビルドに成功した。
- 本番 `dpl_2y3NocQM6W73Se1iccvTg2U8G9a7` を `https://v0-line-blush.vercel.app` へ反映した。管理の休暇・欠勤画面で集計カードと承認待ちを確認し、390px幅で横はみ出しなし、ブラウザエラー0件だった。既存テーブルの監査JSONを利用するためDBマイグレーションはない。

## 2026-08-25 ユーザー一覧の退職者分離

- 管理のユーザー一覧APIで、TSGアカウントに紐づく人事台帳の `payroll_status` と退職日を取得するようにした。アカウントの単なる停止状態と、人事上の退職を区別して扱う。
- ユーザー画面を既定表示の「在籍者」と専用の「退職者」に分け、退職処理済みユーザーを在籍者一覧へ表示しない。退職者側は氏名、LINE表示名、所属、退職日の閲覧専用とし、誤ってアカウント再開や権限変更を行わない構成にした。
- 従業員、勤怠、給与、労務士提出用の履歴は変更・削除しない。DBマイグレーションは不要。
- ESLintはエラー0件（既存警告23件）、`tsc --noEmit`、全79ルートのローカル・Vercel本番ビルドに成功。本番 `dpl_H6uQNTcXKoXdEaG8qoQk5NMT59NR` を固定URLへ反映した。
- 本番データは在籍者25名・退職者1名。退職者は通常一覧に含まれず、専用一覧で退職日を表示し、編集操作がないことを確認した。390px幅で横はみ出しなし、ブラウザエラー0件。

## 2026-08-26 打刻確認からの有給承認をTOP・管理職掲示板へ統一

- 藤田香織さんの通常の本人申請（8月2日全休・8月17日半休）は管理職掲示板へ正常投稿済みだった。一方、8月25日に承認した8月20日・23日分は、打刻なし回答から有給へ確定する別経路が基礎の残日数引当関数だけを呼び、管理職掲示板投稿を行っていなかった。
- ホームTOPの承認待ちAPIも `request_source=employee` の通常申請だけを取得し、打刻確認から作られた有給回答を対象外にしていた。今後は一般スタッフは所属長、管理者本人は佐藤正彦という既存の承認先判定を維持したまま、両経路を同じTOP一覧へ表示する。
- `gw_confirm_workday_resolution` は、有給回答の場合に残日数引当・回答確定・管理職掲示板へのTSG君投稿を同一DBトランザクションで完了する。APIは作成された投稿IDを受けて既存の管理職向けプッシュ通知も送る。二重実行時は既存投稿IDを利用し、重複投稿しない。
- 本番マイグレーション `202608260001_paid_leave_resolution_management_post.sql` を適用し、欠けていた藤田香織さんの8月20日・23日分だけを `TS（管理職）` へTSG君名義で補完した。両申請は既に承認・引当済みだったため、有給残日数の再消化は行っていない。対象経路の未投稿は0件。
- DBロールバック試験で残日数引当・回答確定・掲示板投稿の一体処理と検証データ0件を確認した。Postgres lintはエラー0件、対象ESLintと全体Lintはエラー0件、`tsc --noEmit`、ローカル・Vercel本番ビルドに成功した。
- コミット `c59e021`、本番 `dpl_wxkCSwAYzKJSfrVJg4qMzfgoonVp` を `https://v0-line-blush.vercel.app` へ反映。本番ホームで管理職掲示板の最新投稿と未読2件を確認した。

## 2026-08-26 通知センター

- ホーム上部の丸アイコンへベルを追加し、`/notifications` に自分へのメンション、タスク依頼、自分の投稿へのリアクション、コメント、自分のコメントへの返信を時系列で集約した。すべて・メンション・タスク・リアクション・コメントの5分類で絞り込める。
- 通知内容は複製保存せず、`gw_mentions`、`gw_tasks`、`gw_reactions`、`gw_posts` の正規データからDB関数で組み立てる。リアクション取消、コメント削除、タスク取消は通知一覧にも反映され、コメント内で本人がメンションされた場合はコメント通知との重複を避ける。
- `gw_notification_center_state` はアカウント単位の既読時刻だけを保持する。既存ユーザーは導入時点を既読として初期化し、過去履歴を大量な新着扱いにしない。新規ユーザーは作成時に自動初期化する。ベルは最大 `99+` で表示し、通知画面を開いた時点までを既読にする。
- 本番データを使ったロールバック試験で4分類、内部リンク、件数上限、全ユーザーの既読状態、既読更新を確認した。Postgres lintはエラー0件、対象・全体ESLintはエラー0件（既存警告23件）、`tsc --noEmit`、全80ルートのローカル・Vercel本番ビルドに成功した。
- コミット `eeff82c`、本番 `dpl_6FhDct993Zhz5UBB7ZLgSrg4wozi` を `https://v0-line-blush.vercel.app` へ反映。本番で通知100件、5分類、各遷移、ベル未読200件の `99+` 表示、通知閲覧後の消去、ホーム横はみ出しなし、ブラウザエラー0件を確認した。

## 2026-08-27 確定後有給の柔軟申請と所属長承認

- 本番で武藤志保の新規申請が存在しない原因は、8月28日が確定シフト上の休み希望で勤務割当がなく、本人・管理者APIが申請保存前に拒否していたことだった。承認先は従来どおり道の駅所属長の佐藤ちさとで、誤配はなかった。
- 確定シフトが休みの日も、有給全休を本人または管理者が申請できるよう変更した。所定時間は確定割当、人事の基本勤務、同期間の直近勤務の順に確定し、根拠がない場合だけ登録を止める。勤務枠のない半休は勤務時間を決められないため対象外。
- 申請時点では確定シフトを変更せず、所属長が承認した同一DBトランザクション内で、休み希望を有給へ変更し、有給残数引当、確定シフト表示、労務用仮打刻、管理職掲示板投稿をまとめて反映する。却下時は元のシフトを維持する。
- 社長が一般スタッフ分を代理登録した場合は自動承認せず所属長へ回し、所属長自身が担当スタッフ分を登録した場合だけ即時承認する。管理者代行の承認待ちもホームに表示するよう統一した。
- 有給画面、管理画面、有給取得マニュアルを新しい流れへ更新。SQLは本番DB上のロールバック実行で構文と依存関係を確認し、対象ESLint、`tsc --noEmit`、全体Lint（既存警告のみ）、全80ルートのローカルビルドに成功した。
- 同一日を含む複数部署の確定期間から先頭を選ぶと本人の勤務を見失うため、対象社員の所属部署で期間を厳密に絞り込むよう追加修正した。武藤志保の道の駅期間、8月28日の休み希望、直近勤務8:30〜16:00・休憩60分・実働390分を本番データで照合した。

## 2026-08-27 TSAからのTSG君個別DM連携

- `POST /api/integrations/tsa/direct-message` を追加し、`TSG_INTEGRATION_SECRET` で認証したTSA内部処理から、TSG君名義の個別DMを送信できるようにした。
- 宛先は承認済みユーザーの氏名で一意に特定し、TSG君と宛先の2名だけで構成されたDMであることを送信前に検証する。連携IDと宛先から決定的な投稿IDを生成し、再試行時の二重送信を防ぐ。
- 単体テスト、対象ESLint、`tsc --noEmit`、全81ルートのローカル・Vercel本番ビルドに成功。コミット `1461e24`、本番 `https://ts-groupware-42qykcxgy-hajioxs-projects.vercel.app` を `https://v0-line-blush.vercel.app` へ反映した。
- 原材料AI生成の法令対応完了を藤田香織さんへ送信し、HTTP 201、送信者TSG君、宛先藤田香織を確認した。同じ連携IDの再試行はHTTP 200・重複扱いとなり、新規DMを増やさないことを確認した。

## 2026-08-27 有給申請修正の顛末DM

- 確定後有給の申請拒否と所属部署の確定期間選択不具合について、修正内容、承認経路、武藤志保の8月28日申請が佐藤ちさと承認待ちであることを、TSG君から藤田香織、佐藤正彦、佐藤ちさと、武藤志保へ個別DMで報告した。
- `POST /api/integrations/tsa/direct-message` を使用し、4名それぞれの立場に合わせた本文を送信した。4件とも初回送信成功・重複なしで、API内の氏名一意確認、TSG君との2名DM構成、送信者・本文一致検査を通過した。

## 2026-08-27 FAX受信投稿へのCodex Bridge要約追記

- DocScannerからFAXを受信した時点では、従来どおりPDFから生成したページ画像を掲示板へ即時投稿し、本文に「要約を作成中です」と表示する。添付の確認をAI要約の完了待ちにしない。
- TSA Codex Bridgeの処理完了後は、認証済みPATCHで同じ投稿のAI要約欄だけを更新する。添付画像、投稿者、掲示板、受信IDは変更せず、重複POSTでも完了済み要約を処理中へ戻さない。
- 要約失敗時は同じ投稿へ確認依頼を表示し、FAX画像は保持する。表示組み立ての単体テスト、対象ESLint、`tsc --noEmit`、本番ビルドを確認した。実FAXを使う投稿試験は本番データを増やさないため未実施。

## 2026-08-29 全社員シフト・勤怠照合と未回答差異の管理対応

- `C:\作業用\労務` の従業員毎勤怠集計PDF、2025年・2026年シフト表、TSG本番の8月確定シフト・打刻を氏名別・日別に照合した。8月28日終了時点で実勤務日数が予定より不足するのは、呉東美名子13日、山口光子1日、武藤志保1日。橋本寿恵と角田聖子は予定日に打刻がない日と同月のシフト外出勤が各1日あり、欠勤ではなく勤務日変更候補として分離した。
- 呉東美名子は7月が予定18日・記録勤務8日・日数不足10日、8月28日までが予定16日・記録勤務3日・日数不足13日。8月のうち9日は勤怠備考に「欠勤」と記録済みだが、従来は本人が回答して作られた `gw_workday_resolutions` だけが管理画面と欠勤集計の対象だったため、欠勤0件のままになっていた。
- 管理の「有給・欠勤」に、本人未回答でも確定シフトと打刻から検出した勤怠差異を表示する。予定日に打刻がなく同月にシフト外出勤がある場合は候補日も併記し、日付差異と実勤務日数不足を分けて確認できる。
- 所属長または管理権限者は、未回答差異を「欠勤」または「勤務日変更」として直接確定できる。欠勤確定前には給与・出勤率へ反映される警告を出し、確定結果は勤怠備考、欠勤集計、出勤率、労務士提出用データへ同時に反映する。有給は既存の所属長承認経路へ接続し、自動欠勤確定は行わない。
- 過去の労務士PDFは社員によって通常勤務全体ではなく残業等だけが記録された月がある。石井瑞季の2026年7月は17:30以降の記録8日だけで、シフト差を欠勤と断定できない。2026年6月勤務分の確定資料もないため、旧資料だけを使った法定80%判定は未確定として扱う。
